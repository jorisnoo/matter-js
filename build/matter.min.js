/**
 * matter-js 1.0.0 by @liabru
 * http://brm.io/matter-js/
 * License MIT
 */
var m = class T {
  static _baseDelta = 16.666666666666668;
  static _nextId = 0;
  static _seed = 0;
  static _warnedOnce = {};
  static _decomp = null;
  static logLevel = 1;
  static extend(t, e, ...s) {
    let i, n;
    typeof e == "boolean" ? (n = e, i = s) : (n = !0, i = [e, ...s]);
    for (let a = 0; a < i.length; a++) {
      const o = i[a];
      if (o) for (const r in o) n && o[r] && o[r].constructor === Object && (!t[r] || t[r].constructor === Object) ? (t[r] = t[r] || {}, T.extend(t[r], n, o[r])) : t[r] = o[r];
    }
    return t;
  }
  static clone(t, e) {
    return T.extend({}, e, t);
  }
  static get(t, e, s, i) {
    e = e.split(".").slice(s, i);
    for (let n = 0; n < e.length; n += 1) t = t[e[n]];
    return t;
  }
  static set(t, e, s, i, n) {
    const a = e.split(".").slice(i, n);
    return T.get(t, e, 0, -1)[a[a.length - 1]] = s, s;
  }
  static shuffle(t) {
    for (let e = t.length - 1; e > 0; e--) {
      const s = Math.floor(T.random() * (e + 1)), i = t[e];
      t[e] = t[s], t[s] = i;
    }
    return t;
  }
  static choose(t) {
    return t[Math.floor(T.random() * t.length)];
  }
  static isElement(t) {
    return typeof HTMLElement < "u" ? t instanceof HTMLElement : !!(t && t.nodeType && t.nodeName);
  }
  static clamp(t, e, s) {
    return t < e ? e : t > s ? s : t;
  }
  static sign(t) {
    return t < 0 ? -1 : 1;
  }
  static now() {
    return typeof performance < "u" && performance.now ? performance.now() : Date.now();
  }
  static random(t = 0, e = 1) {
    return t + T._seededRandom() * (e - t);
  }
  static _seededRandom() {
    return T._seed = (T._seed * 9301 + 49297) % 233280, T._seed / 233280;
  }
  static log(...t) {
    console && T.logLevel > 0 && T.logLevel <= 3 && console.log("matter-js:", ...t);
  }
  static info(...t) {
    console && T.logLevel > 0 && T.logLevel <= 2 && console.info("matter-js:", ...t);
  }
  static warn(...t) {
    console && T.logLevel > 0 && T.logLevel <= 3 && console.warn("matter-js:", ...t);
  }
  static warnOnce(...t) {
    const e = t.join(" ");
    T._warnedOnce[e] || (T.warn(e), T._warnedOnce[e] = !0);
  }
  static deprecated(t, e, s) {
    t[e] = T.chain(function() {
      T.warnOnce("🔅 deprecated 🔅", s);
    }, t[e]);
  }
  static nextId() {
    return T._nextId++;
  }
  static topologicalSort(t) {
    const e = [], s = [], i = [];
    for (const n in t) !s[n] && !i[n] && T._topologicalSort(n, s, i, t, e);
    return e;
  }
  static _topologicalSort(t, e, s, i, n) {
    const a = i[t] || [];
    s[t] = !0;
    for (let o = 0; o < a.length; o += 1) {
      const r = a[o];
      s[r] || e[r] || T._topologicalSort(r, e, s, i, n);
    }
    s[t] = !1, e[t] = !0, n.push(t);
  }
  static chain(...t) {
    const e = [];
    for (let i = 0; i < t.length; i += 1) {
      const n = t[i];
      n._chained ? e.push(...n._chained) : e.push(n);
    }
    const s = function(...i) {
      let n;
      for (let a = 0; a < e.length; a += 1) {
        const o = e[a].apply(n, i);
        typeof o < "u" && (n = o);
      }
      return n;
    };
    return s._chained = e, s;
  }
  static chainPathBefore(t, e, s) {
    return T.set(t, e, T.chain(s, T.get(t, e)));
  }
  static chainPathAfter(t, e, s) {
    return T.set(t, e, T.chain(T.get(t, e), s));
  }
  static setDecomp(t) {
    T._decomp = t;
  }
  static getDecomp() {
    let t = T._decomp;
    try {
      !t && typeof window < "u" && (t = window.decomp), !t && typeof global < "u" && (t = global.decomp);
    } catch {
      t = null;
    }
    return t;
  }
}, Ht = class B {
  static _registry = {};
  static register(t) {
    if (B.isPlugin(t) || m.warn("Plugin.register:", B.toString(t), "does not implement all required fields."), t.name in B._registry) {
      const e = B._registry[t.name], s = B.versionParse(t.version).number, i = B.versionParse(e.version).number;
      s > i ? (m.warn("Plugin.register:", B.toString(e), "was upgraded to", B.toString(t)), B._registry[t.name] = t) : s < i ? m.warn("Plugin.register:", B.toString(e), "can not be downgraded to", B.toString(t)) : t !== e && m.warn("Plugin.register:", B.toString(t), "is already registered to different plugin object");
    } else B._registry[t.name] = t;
    return t;
  }
  static resolve(t) {
    return B._registry[B.dependencyParse(t).name];
  }
  static toString(t) {
    return typeof t == "string" ? t : `${t.name || "anonymous"}@${t.version || t.range || "0.0.0"}`;
  }
  static isPlugin(t) {
    return t && t.name && t.version && t.install;
  }
  static isUsed(t, e) {
    return t.used.indexOf(e) > -1;
  }
  static isFor(t, e) {
    const s = t.for && B.dependencyParse(t.for);
    return !t.for || e.name === s.name && B.versionSatisfies(e.version, s.range);
  }
  static use(t, e) {
    if (t.uses = (t.uses || []).concat(e || []), t.uses.length === 0) {
      m.warn("Plugin.use:", B.toString(t), "does not specify any dependencies to install.");
      return;
    }
    const s = B.dependencies(t), i = m.topologicalSort(s), n = [];
    for (let a = 0; a < i.length; a += 1) {
      if (i[a] === t.name) continue;
      const o = B.resolve(i[a]);
      if (!o) {
        n.push(`❌ ${i[a]}`);
        continue;
      }
      B.isUsed(t, o.name) || (B.isFor(o, t) || (m.warn("Plugin.use:", B.toString(o), "is for", o.for, "but installed on", `${B.toString(t)}.`), o._warned = !0), o.install ? o.install(t) : (m.warn("Plugin.use:", B.toString(o), "does not specify an install function."), o._warned = !0), o._warned ? (n.push(`🔶 ${B.toString(o)}`), delete o._warned) : n.push(`✅ ${B.toString(o)}`), t.used.push(o.name));
    }
    n.length > 0 && m.info(n.join("  "));
  }
  static dependencies(t, e = {}) {
    const s = B.dependencyParse(t), i = s.name;
    if (!(i in e)) {
      t = B.resolve(t) || t, e[i] = (t.uses || []).map((n) => {
        B.isPlugin(n) && B.register(n);
        const a = B.dependencyParse(n), o = B.resolve(n);
        return o && !B.versionSatisfies(o.version, a.range) ? (m.warn("Plugin.dependencies:", B.toString(o), "does not satisfy", B.toString(a), "used by", `${B.toString(s)}.`), o._warned = !0, t._warned = !0) : o || (m.warn("Plugin.dependencies:", B.toString(n), "used by", B.toString(s), "could not be resolved."), t._warned = !0), a.name;
      });
      for (let n = 0; n < e[i].length; n += 1) B.dependencies(e[i][n], e);
      return e;
    }
  }
  static dependencyParse(t) {
    return typeof t == "string" ? (/^[\w-]+(@(\*|[\^~]?\d+\.\d+\.\d+(-[0-9A-Za-z-+]+)?))?$/.test(t) || m.warn("Plugin.dependencyParse:", t, "is not a valid dependency string."), {
      name: t.split("@")[0],
      range: t.split("@")[1] || "*"
    }) : {
      name: t.name,
      range: t.range || t.version
    };
  }
  static versionParse(t) {
    const e = /^(\*)|(\^|~|>=|>)?\s*((\d+)\.(\d+)\.(\d+))(-[0-9A-Za-z-+]+)?$/;
    e.test(t) || m.warn("Plugin.versionParse:", t, "is not a valid version or range.");
    const s = e.exec(t), i = Number(s[4]), n = Number(s[5]), a = Number(s[6]);
    return {
      isRange: !!(s[1] || s[2]),
      version: s[3],
      range: t,
      operator: s[1] || s[2] || "",
      major: i,
      minor: n,
      patch: a,
      parts: [
        i,
        n,
        a
      ],
      prerelease: s[7],
      number: i * 1e8 + n * 1e4 + a
    };
  }
  static versionSatisfies(t, e) {
    e = e || "*";
    const s = B.versionParse(e), i = B.versionParse(t);
    if (s.isRange) {
      if (s.operator === "*" || t === "*") return !0;
      if (s.operator === ">") return i.number > s.number;
      if (s.operator === ">=") return i.number >= s.number;
      if (s.operator === "~") return i.major === s.major && i.minor === s.minor && i.patch >= s.patch;
      if (s.operator === "^")
        return s.major > 0 ? i.major === s.major && i.number >= s.number : s.minor > 0 ? i.minor === s.minor && i.patch >= s.patch : i.patch === s.patch;
    }
    return t === e || t === "*";
  }
}, V = class ht {
  static name = "matter-js";
  static version = "1.0.0";
  static uses = [];
  static used = [];
  static use(...t) {
    Ht.use(ht, t);
  }
  static before(t, e) {
    return t = t.replace(/^Matter./, ""), m.chainPathBefore(ht, t, e);
  }
  static after(t, e) {
    return t = t.replace(/^Matter./, ""), m.chainPathAfter(ht, t, e);
  }
}, v = class G {
  static create(t, e) {
    return {
      x: t || 0,
      y: e || 0
    };
  }
  static clone(t) {
    return {
      x: t.x,
      y: t.y
    };
  }
  static magnitude(t) {
    return Math.sqrt(t.x * t.x + t.y * t.y);
  }
  static magnitudeSquared(t) {
    return t.x * t.x + t.y * t.y;
  }
  static rotate(t, e, s) {
    const i = Math.cos(e), n = Math.sin(e);
    s || (s = {});
    const a = t.x * i - t.y * n;
    return s.y = t.x * n + t.y * i, s.x = a, s;
  }
  static rotateAbout(t, e, s, i) {
    const n = Math.cos(e), a = Math.sin(e);
    i || (i = {});
    const o = s.x + ((t.x - s.x) * n - (t.y - s.y) * a);
    return i.y = s.y + ((t.x - s.x) * a + (t.y - s.y) * n), i.x = o, i;
  }
  static normalise(t) {
    const e = G.magnitude(t);
    return e === 0 ? {
      x: 0,
      y: 0
    } : {
      x: t.x / e,
      y: t.y / e
    };
  }
  static dot(t, e) {
    return t.x * e.x + t.y * e.y;
  }
  static cross(t, e) {
    return t.x * e.y - t.y * e.x;
  }
  static cross3(t, e, s) {
    return (e.x - t.x) * (s.y - t.y) - (e.y - t.y) * (s.x - t.x);
  }
  static add(t, e, s) {
    return s || (s = {}), s.x = t.x + e.x, s.y = t.y + e.y, s;
  }
  static sub(t, e, s) {
    return s || (s = {}), s.x = t.x - e.x, s.y = t.y - e.y, s;
  }
  static mult(t, e) {
    return {
      x: t.x * e,
      y: t.y * e
    };
  }
  static div(t, e) {
    return {
      x: t.x / e,
      y: t.y / e
    };
  }
  static perp(t, e) {
    return e = e === !0 ? -1 : 1, {
      x: e * -t.y,
      y: e * t.x
    };
  }
  static neg(t) {
    return {
      x: -t.x,
      y: -t.y
    };
  }
  static angle(t, e) {
    return Math.atan2(e.y - t.y, e.x - t.x);
  }
  static _temp = [
    G.create(),
    G.create(),
    G.create(),
    G.create(),
    G.create(),
    G.create()
  ];
}, _ = class {
  static fromVertices(D) {
    const t = {};
    for (let e = 0; e < D.length; e++) {
      const s = (e + 1) % D.length, i = v.normalise({
        x: D[s].y - D[e].y,
        y: D[e].x - D[s].x
      });
      let n = i.y === 0 ? 1 / 0 : i.x / i.y;
      n = n.toFixed(3).toString(), t[n] = i;
    }
    return Object.values(t);
  }
  static rotate(D, t) {
    if (t === 0) return;
    const e = Math.cos(t), s = Math.sin(t);
    for (let i = 0; i < D.length; i++) {
      const n = D[i];
      let a;
      a = n.x * e - n.y * s, n.y = n.x * s + n.y * e, n.x = a;
    }
  }
}, C = class ot {
  static create(t, e) {
    const s = [];
    for (let i = 0; i < t.length; i++) {
      const n = t[i], a = {
        x: n.x,
        y: n.y,
        index: i,
        body: e,
        isInternal: !1
      };
      s.push(a);
    }
    return s;
  }
  static fromPath(t, e) {
    const s = /L?\s*([-\d.e]+)[\s,]*([-\d.e]+)*/gi, i = [];
    return t.replace(s, (n, a, o) => {
      i.push({
        x: parseFloat(a),
        y: parseFloat(o)
      });
    }), ot.create(i, e);
  }
  static centre(t) {
    const e = ot.area(t, !0);
    let s = {
      x: 0,
      y: 0
    }, i, n, a;
    for (let o = 0; o < t.length; o++)
      a = (o + 1) % t.length, i = v.cross(t[o], t[a]), n = v.mult(v.add(t[o], t[a]), i), s = v.add(s, n);
    return v.div(s, 6 * e);
  }
  static mean(t) {
    const e = {
      x: 0,
      y: 0
    };
    for (let s = 0; s < t.length; s++)
      e.x += t[s].x, e.y += t[s].y;
    return v.div(e, t.length);
  }
  static area(t, e) {
    let s = 0, i = t.length - 1;
    for (let n = 0; n < t.length; n++)
      s += (t[i].x - t[n].x) * (t[i].y + t[n].y), i = n;
    return e ? s / 2 : Math.abs(s) / 2;
  }
  static inertia(t, e) {
    let s = 0, i = 0, n, a;
    const o = t;
    for (let r = 0; r < o.length; r++)
      a = (r + 1) % o.length, n = Math.abs(v.cross(o[a], o[r])), s += n * (v.dot(o[a], o[a]) + v.dot(o[a], o[r]) + v.dot(o[r], o[r])), i += n;
    return e / 6 * (s / i);
  }
  static translate(t, e, s = 1) {
    const i = t.length, n = e.x * s, a = e.y * s;
    let o;
    for (o = 0; o < i; o++)
      t[o].x += n, t[o].y += a;
    return t;
  }
  static rotate(t, e, s) {
    if (e === 0) return;
    const i = Math.cos(e), n = Math.sin(e), a = s.x, o = s.y, r = t.length;
    let l, c, f, p;
    for (p = 0; p < r; p++)
      l = t[p], c = l.x - a, f = l.y - o, l.x = a + (c * i - f * n), l.y = o + (c * n + f * i);
    return t;
  }
  static contains(t, e) {
    const s = e.x, i = e.y, n = t.length;
    let a = t[n - 1], o;
    for (let r = 0; r < n; r++) {
      if (o = t[r], (s - a.x) * (o.y - a.y) + (i - a.y) * (a.x - o.x) > 0) return !1;
      a = o;
    }
    return !0;
  }
  static scale(t, e, s, i) {
    if (e === 1 && s === 1) return t;
    i = i || ot.centre(t);
    let n, a;
    for (let o = 0; o < t.length; o++)
      n = t[o], a = v.sub(n, i), t[o].x = i.x + a.x * e, t[o].y = i.y + a.y * s;
    return t;
  }
  static chamfer(t, e, s = -1, i = 2, n = 14) {
    typeof e == "number" ? e = [e] : e = e || [8];
    const a = [];
    for (let o = 0; o < t.length; o++) {
      const r = t[o - 1 >= 0 ? o - 1 : t.length - 1], l = t[o], c = t[(o + 1) % t.length], f = e[o < e.length ? o : e.length - 1];
      if (f === 0) {
        a.push(l);
        continue;
      }
      const p = v.normalise({
        x: l.y - r.y,
        y: r.x - l.x
      }), u = v.normalise({
        x: c.y - l.y,
        y: l.x - c.x
      }), g = Math.sqrt(2 * Math.pow(f, 2)), y = v.mult(m.clone(p), f), h = v.normalise(v.mult(v.add(p, u), 0.5)), d = v.sub(l, v.mult(h, g));
      let x = s;
      s === -1 && (x = Math.pow(f, 0.32) * 1.75), x = m.clamp(x, i, n), x % 2 === 1 && (x += 1);
      const S = Math.acos(v.dot(p, u)) / x;
      for (let P = 0; P < x; P++) a.push(v.add(v.rotate(y, S * P), d));
    }
    return a;
  }
  static clockwiseSort(t) {
    const e = ot.mean(t);
    return t.sort((s, i) => v.angle(e, s) - v.angle(e, i)), t;
  }
  static isConvex(t) {
    let e = 0, s, i, n, a;
    const o = t.length;
    if (o < 3) return null;
    for (s = 0; s < o; s++)
      if (i = (s + 1) % o, n = (s + 2) % o, a = (t[i].x - t[s].x) * (t[n].y - t[i].y), a -= (t[i].y - t[s].y) * (t[n].x - t[i].x), a < 0 ? e |= 1 : a > 0 && (e |= 2), e === 3) return !1;
    return e !== 0 ? !0 : null;
  }
  static hull(t) {
    const e = [], s = [];
    let i, n;
    for (t = t.slice(0), t.sort((a, o) => {
      const r = a.x - o.x;
      return r !== 0 ? r : a.y - o.y;
    }), n = 0; n < t.length; n += 1) {
      for (i = t[n]; s.length >= 2 && v.cross3(s[s.length - 2], s[s.length - 1], i) <= 0; ) s.pop();
      s.push(i);
    }
    for (n = t.length - 1; n >= 0; n -= 1) {
      for (i = t[n]; e.length >= 2 && v.cross3(e[e.length - 2], e[e.length - 1], i) <= 0; ) e.pop();
      e.push(i);
    }
    return e.pop(), s.pop(), e.concat(s);
  }
}, E = class {
  static on(D, t, e) {
    const s = t.split(" ");
    let i;
    for (let n = 0; n < s.length; n++)
      i = s[n], D.events = D.events || {}, D.events[i] = D.events[i] || [], D.events[i].push(e);
    return e;
  }
  static off(D, t, e) {
    if (!t) {
      D.events = {};
      return;
    }
    typeof t == "function" && (e = t, t = Object.keys(D.events).join(" "));
    const s = t.split(" ");
    for (let i = 0; i < s.length; i++) {
      const n = D.events[s[i]], a = [];
      if (e && n)
        for (let o = 0; o < n.length; o++) n[o] !== e && a.push(n[o]);
      D.events[s[i]] = a;
    }
  }
  static trigger(D, t, e) {
    let s, i, n, a;
    const o = D.events;
    if (o && Object.keys(o).length > 0) {
      e || (e = {}), s = t.split(" ");
      for (let r = 0; r < s.length; r++)
        if (i = s[r], n = o[i], n) {
          a = m.clone(e, !1), a.name = i, a.source = D;
          for (let l = 0; l < n.length; l++) n[l].call(D, a);
        }
    }
  }
}, Z = class Q {
  static _motionWakeThreshold = 0.18;
  static _motionSleepThreshold = 0.08;
  static _minBias = 0.9;
  static update(t, e) {
    const s = e / m._baseDelta, i = Q._motionSleepThreshold;
    for (let n = 0; n < t.length; n++) {
      const a = t[n], o = q.getSpeed(a), r = q.getAngularSpeed(a), l = o * o + r * r;
      if (a.force.x !== 0 || a.force.y !== 0) {
        Q.set(a, !1);
        continue;
      }
      const c = Math.min(a.motion, l), f = Math.max(a.motion, l);
      a.motion = Q._minBias * c + (1 - Q._minBias) * f, a.sleepThreshold > 0 && a.motion < i ? (a.sleepCounter += 1, a.sleepCounter >= a.sleepThreshold / s && Q.set(a, !0)) : a.sleepCounter > 0 && (a.sleepCounter -= 1);
    }
  }
  static afterCollisions(t) {
    const e = Q._motionSleepThreshold;
    for (let s = 0; s < t.length; s++) {
      const i = t[s];
      if (!i.isActive) continue;
      const n = i.collision, a = n.bodyA.parent, o = n.bodyB.parent;
      if (!(a.isSleeping && o.isSleeping || a.isStatic || o.isStatic) && (a.isSleeping || o.isSleeping)) {
        const r = a.isSleeping && !a.isStatic ? a : o, l = r === a ? o : a;
        !r.isStatic && l.motion > e && Q.set(r, !1);
      }
    }
  }
  static set(t, e) {
    const s = t.isSleeping;
    e ? (t.isSleeping = !0, t.sleepCounter = t.sleepThreshold, t.positionImpulse.x = 0, t.positionImpulse.y = 0, t.positionPrev.x = t.position.x, t.positionPrev.y = t.position.y, t.anglePrev = t.angle, t.speed = 0, t.angularSpeed = 0, t.motion = 0, s || E.trigger(t, "sleepStart")) : (t.isSleeping = !1, t.sleepCounter = 0, s && E.trigger(t, "sleepEnd"));
  }
}, H = class Ot {
  static create(t) {
    const e = {
      min: {
        x: 0,
        y: 0
      },
      max: {
        x: 0,
        y: 0
      }
    };
    return t && Ot.update(e, t), e;
  }
  static update(t, e, s) {
    t.min.x = 1 / 0, t.max.x = -1 / 0, t.min.y = 1 / 0, t.max.y = -1 / 0;
    for (let i = 0; i < e.length; i++) {
      const n = e[i];
      n.x > t.max.x && (t.max.x = n.x), n.x < t.min.x && (t.min.x = n.x), n.y > t.max.y && (t.max.y = n.y), n.y < t.min.y && (t.min.y = n.y);
    }
    s && (s.x > 0 ? t.max.x += s.x : t.min.x += s.x, s.y > 0 ? t.max.y += s.y : t.min.y += s.y);
  }
  static contains(t, e) {
    return e.x >= t.min.x && e.x <= t.max.x && e.y >= t.min.y && e.y <= t.max.y;
  }
  static overlaps(t, e) {
    return t.min.x <= e.max.x && t.max.x >= e.min.x && t.max.y >= e.min.y && t.min.y <= e.max.y;
  }
  static translate(t, e) {
    t.min.x += e.x, t.max.x += e.x, t.min.y += e.y, t.max.y += e.y;
  }
  static shift(t, e) {
    const s = t.max.x - t.min.x, i = t.max.y - t.min.y;
    t.min.x = e.x, t.max.x = e.x + s, t.min.y = e.y, t.max.y = e.y + i;
  }
}, q = class w {
  static _timeCorrection = !0;
  static _inertiaScale = 4;
  static _nextCollidingGroupId = 1;
  static _nextNonCollidingGroupId = -1;
  static _nextCategory = 1;
  static _baseDelta = 1e3 / 60;
  constructor(t = {}) {
    const e = {
      id: m.nextId(),
      type: "body",
      label: "Body",
      parts: [],
      plugin: {},
      angle: 0,
      vertices: C.fromPath("L 0 0 L 40 0 L 40 40 L 0 40"),
      position: {
        x: 0,
        y: 0
      },
      force: {
        x: 0,
        y: 0
      },
      torque: 0,
      positionImpulse: {
        x: 0,
        y: 0
      },
      constraintImpulse: {
        x: 0,
        y: 0,
        angle: 0
      },
      totalContacts: 0,
      speed: 0,
      angularSpeed: 0,
      velocity: {
        x: 0,
        y: 0
      },
      angularVelocity: 0,
      isSensor: !1,
      isStatic: !1,
      isSleeping: !1,
      motion: 0,
      sleepThreshold: 60,
      density: 1e-3,
      restitution: 0,
      friction: 0.1,
      frictionStatic: 0.5,
      frictionAir: 0.01,
      collisionFilter: {
        category: 1,
        mask: 4294967295,
        group: 0
      },
      slop: 0.05,
      timeScale: 1,
      render: {
        visible: !0,
        opacity: 1,
        strokeStyle: null,
        fillStyle: null,
        lineWidth: null,
        sprite: {
          xScale: 1,
          yScale: 1,
          xOffset: 0,
          yOffset: 0
        }
      },
      events: null,
      bounds: null,
      chamfer: null,
      circleRadius: 0,
      positionPrev: null,
      anglePrev: 0,
      parent: null,
      axes: null,
      area: 0,
      mass: 0,
      inertia: 0,
      deltaTime: 16.666666666666668,
      _original: null
    };
    Object.assign(this, m.extend(e, t)), w._initProperties(this, t);
  }
  static create(t) {
    return new w(t);
  }
  static nextGroup(t) {
    return t ? w._nextNonCollidingGroupId-- : w._nextCollidingGroupId++;
  }
  static nextCategory() {
    return w._nextCategory = w._nextCategory << 1, w._nextCategory;
  }
  static _initProperties(t, e = {}) {
    w.set(t, {
      bounds: t.bounds || H.create(t.vertices),
      positionPrev: t.positionPrev || v.clone(t.position),
      anglePrev: t.anglePrev || t.angle,
      vertices: t.vertices,
      parts: t.parts || [t],
      isStatic: t.isStatic,
      isSleeping: t.isSleeping,
      parent: t.parent || t
    }), C.rotate(t.vertices, t.angle, t.position), _.rotate(t.axes, t.angle), H.update(t.bounds, t.vertices, t.velocity), w.set(t, {
      axes: e.axes || t.axes,
      area: e.area || t.area,
      mass: e.mass || t.mass,
      inertia: e.inertia || t.inertia
    });
    const s = t.isStatic ? "#14151f" : m.choose([
      "#f19648",
      "#f5d259",
      "#f55a3c",
      "#063e7b",
      "#ececd1"
    ]), i = t.isStatic ? "#555" : "#ccc", n = t.isStatic && t.render.fillStyle === null ? 1 : 0;
    t.render.fillStyle = t.render.fillStyle || s, t.render.strokeStyle = t.render.strokeStyle || i, t.render.lineWidth = t.render.lineWidth || n, t.render.sprite.xOffset += -(t.bounds.min.x - t.position.x) / (t.bounds.max.x - t.bounds.min.x), t.render.sprite.yOffset += -(t.bounds.min.y - t.position.y) / (t.bounds.max.y - t.bounds.min.y);
  }
  static set(t, e, s) {
    let i;
    typeof e == "string" && (i = e, e = {}, e[i] = s);
    for (i in e)
      if (Object.prototype.hasOwnProperty.call(e, i))
        switch (s = e[i], i) {
          case "isStatic":
            w.setStatic(t, s);
            break;
          case "isSleeping":
            Z.set(t, s);
            break;
          case "mass":
            w.setMass(t, s);
            break;
          case "density":
            w.setDensity(t, s);
            break;
          case "inertia":
            w.setInertia(t, s);
            break;
          case "vertices":
            w.setVertices(t, s);
            break;
          case "position":
            w.setPosition(t, s);
            break;
          case "angle":
            w.setAngle(t, s);
            break;
          case "velocity":
            w.setVelocity(t, s);
            break;
          case "angularVelocity":
            w.setAngularVelocity(t, s);
            break;
          case "speed":
            w.setSpeed(t, s);
            break;
          case "angularSpeed":
            w.setAngularSpeed(t, s);
            break;
          case "parts":
            w.setParts(t, s);
            break;
          case "centre":
            w.setCentre(t, s);
            break;
          default:
            t[i] = s;
        }
  }
  static setStatic(t, e) {
    for (let s = 0; s < t.parts.length; s++) {
      const i = t.parts[s];
      e ? (i.isStatic || (i._original = {
        restitution: i.restitution,
        friction: i.friction,
        mass: i.mass,
        inertia: i.inertia,
        density: i.density,
        inverseMass: i.inverseMass,
        inverseInertia: i.inverseInertia
      }), i.restitution = 0, i.friction = 1, i.mass = i.inertia = i.density = 1 / 0, i.inverseMass = i.inverseInertia = 0, i.positionPrev.x = i.position.x, i.positionPrev.y = i.position.y, i.anglePrev = i.angle, i.angularVelocity = 0, i.speed = 0, i.angularSpeed = 0, i.motion = 0) : i._original && (i.restitution = i._original.restitution, i.friction = i._original.friction, i.mass = i._original.mass, i.inertia = i._original.inertia, i.density = i._original.density, i.inverseMass = i._original.inverseMass, i.inverseInertia = i._original.inverseInertia, i._original = null), i.isStatic = e;
    }
  }
  static setMass(t, e) {
    t.inertia = t.inertia / (t.mass / 6) * (e / 6), t.inverseInertia = 1 / t.inertia, t.mass = e, t.inverseMass = 1 / t.mass, t.density = t.mass / t.area;
  }
  static setDensity(t, e) {
    w.setMass(t, e * t.area), t.density = e;
  }
  static setInertia(t, e) {
    t.inertia = e, t.inverseInertia = 1 / t.inertia;
  }
  static setVertices(t, e) {
    e[0].body === t ? t.vertices = e : t.vertices = C.create(e, t), t.axes = _.fromVertices(t.vertices), t.area = C.area(t.vertices), w.setMass(t, t.density * t.area);
    const s = C.centre(t.vertices);
    C.translate(t.vertices, s, -1), w.setInertia(t, w._inertiaScale * C.inertia(t.vertices, t.mass)), C.translate(t.vertices, t.position), H.update(t.bounds, t.vertices, t.velocity);
  }
  static setParts(t, e, s) {
    let i;
    for (e = e.slice(0), t.parts.length = 0, t.parts.push(t), t.parent = t, i = 0; i < e.length; i++) {
      const a = e[i];
      a !== t && (a.parent = t, t.parts.push(a));
    }
    if (t.parts.length === 1) return;
    if (s = s ?? !0, s) {
      let a = [];
      for (i = 0; i < e.length; i++) a = a.concat(e[i].vertices);
      C.clockwiseSort(a);
      const o = C.hull(a), r = C.centre(o);
      w.setVertices(t, o), C.translate(t.vertices, r);
    }
    const n = w._totalProperties(t);
    t.area = n.area, t.parent = t, t.position.x = n.centre.x, t.position.y = n.centre.y, t.positionPrev.x = n.centre.x, t.positionPrev.y = n.centre.y, w.setMass(t, n.mass), w.setInertia(t, n.inertia), w.setPosition(t, n.centre);
  }
  static setCentre(t, e, s) {
    s ? (t.positionPrev.x += e.x, t.positionPrev.y += e.y, t.position.x += e.x, t.position.y += e.y) : (t.positionPrev.x = e.x - (t.position.x - t.positionPrev.x), t.positionPrev.y = e.y - (t.position.y - t.positionPrev.y), t.position.x = e.x, t.position.y = e.y);
  }
  static setPosition(t, e, s) {
    const i = v.sub(e, t.position);
    s ? (t.positionPrev.x = t.position.x, t.positionPrev.y = t.position.y, t.velocity.x = i.x, t.velocity.y = i.y, t.speed = v.magnitude(i)) : (t.positionPrev.x += i.x, t.positionPrev.y += i.y);
    for (let n = 0; n < t.parts.length; n++) {
      const a = t.parts[n];
      a.position.x += i.x, a.position.y += i.y, C.translate(a.vertices, i), H.update(a.bounds, a.vertices, t.velocity);
    }
  }
  static setAngle(t, e, s) {
    const i = e - t.angle;
    s ? (t.anglePrev = t.angle, t.angularVelocity = i, t.angularSpeed = Math.abs(i)) : t.anglePrev += i;
    for (let n = 0; n < t.parts.length; n++) {
      const a = t.parts[n];
      a.angle += i, C.rotate(a.vertices, i, t.position), _.rotate(a.axes, i), H.update(a.bounds, a.vertices, t.velocity), n > 0 && v.rotateAbout(a.position, i, t.position, a.position);
    }
  }
  static setVelocity(t, e) {
    const s = t.deltaTime / w._baseDelta;
    t.positionPrev.x = t.position.x - e.x * s, t.positionPrev.y = t.position.y - e.y * s, t.velocity.x = (t.position.x - t.positionPrev.x) / s, t.velocity.y = (t.position.y - t.positionPrev.y) / s, t.speed = v.magnitude(t.velocity);
  }
  static getVelocity(t) {
    const e = w._baseDelta / t.deltaTime;
    return {
      x: (t.position.x - t.positionPrev.x) * e,
      y: (t.position.y - t.positionPrev.y) * e
    };
  }
  static getSpeed(t) {
    return v.magnitude(w.getVelocity(t));
  }
  static setSpeed(t, e) {
    w.setVelocity(t, v.mult(v.normalise(w.getVelocity(t)), e));
  }
  static setAngularVelocity(t, e) {
    const s = t.deltaTime / w._baseDelta;
    t.anglePrev = t.angle - e * s, t.angularVelocity = (t.angle - t.anglePrev) / s, t.angularSpeed = Math.abs(t.angularVelocity);
  }
  static getAngularVelocity(t) {
    return (t.angle - t.anglePrev) * w._baseDelta / t.deltaTime;
  }
  static getAngularSpeed(t) {
    return Math.abs(w.getAngularVelocity(t));
  }
  static setAngularSpeed(t, e) {
    w.setAngularVelocity(t, m.sign(w.getAngularVelocity(t)) * e);
  }
  static translate(t, e, s) {
    w.setPosition(t, v.add(t.position, e), s);
  }
  static rotate(t, e, s, i) {
    if (!s) w.setAngle(t, t.angle + e, i);
    else {
      const n = Math.cos(e), a = Math.sin(e), o = t.position.x - s.x, r = t.position.y - s.y;
      w.setPosition(t, {
        x: s.x + (o * n - r * a),
        y: s.y + (o * a + r * n)
      }, i), w.setAngle(t, t.angle + e, i);
    }
  }
  static scale(t, e, s, i) {
    let n = 0, a = 0;
    i = i || t.position;
    for (let o = 0; o < t.parts.length; o++) {
      const r = t.parts[o];
      C.scale(r.vertices, e, s, i), r.axes = _.fromVertices(r.vertices), r.area = C.area(r.vertices), w.setMass(r, t.density * r.area), C.translate(r.vertices, {
        x: -r.position.x,
        y: -r.position.y
      }), w.setInertia(r, w._inertiaScale * C.inertia(r.vertices, r.mass)), C.translate(r.vertices, {
        x: r.position.x,
        y: r.position.y
      }), o > 0 && (n += r.area, a += r.inertia), r.position.x = i.x + (r.position.x - i.x) * e, r.position.y = i.y + (r.position.y - i.y) * s, H.update(r.bounds, r.vertices, t.velocity);
    }
    t.parts.length > 1 && (t.area = n, t.isStatic || (w.setMass(t, t.density * n), w.setInertia(t, a))), t.circleRadius && (e === s ? t.circleRadius *= e : t.circleRadius = null);
  }
  static update(t, e) {
    e = (e ?? 1e3 / 60) * t.timeScale;
    const s = e * e, i = w._timeCorrection ? e / (t.deltaTime || e) : 1, n = 1 - t.frictionAir * (e / m._baseDelta), a = (t.position.x - t.positionPrev.x) * i, o = (t.position.y - t.positionPrev.y) * i;
    t.velocity.x = a * n + t.force.x / t.mass * s, t.velocity.y = o * n + t.force.y / t.mass * s, t.positionPrev.x = t.position.x, t.positionPrev.y = t.position.y, t.position.x += t.velocity.x, t.position.y += t.velocity.y, t.deltaTime = e, t.angularVelocity = (t.angle - t.anglePrev) * n * i + t.torque / t.inertia * s, t.anglePrev = t.angle, t.angle += t.angularVelocity;
    for (let r = 0; r < t.parts.length; r++) {
      const l = t.parts[r];
      C.translate(l.vertices, t.velocity), r > 0 && (l.position.x += t.velocity.x, l.position.y += t.velocity.y), t.angularVelocity !== 0 && (C.rotate(l.vertices, t.angularVelocity, t.position), _.rotate(l.axes, t.angularVelocity), r > 0 && v.rotateAbout(l.position, t.angularVelocity, t.position, l.position)), H.update(l.bounds, l.vertices, t.velocity);
    }
  }
  static updateVelocities(t) {
    const e = w._baseDelta / t.deltaTime, s = t.velocity;
    s.x = (t.position.x - t.positionPrev.x) * e, s.y = (t.position.y - t.positionPrev.y) * e, t.speed = Math.sqrt(s.x * s.x + s.y * s.y), t.angularVelocity = (t.angle - t.anglePrev) * e, t.angularSpeed = Math.abs(t.angularVelocity);
  }
  static applyForce(t, e, s) {
    const i = {
      x: e.x - t.position.x,
      y: e.y - t.position.y
    };
    t.force.x += s.x, t.force.y += s.y, t.torque += i.x * s.y - i.y * s.x;
  }
  static _totalProperties(t) {
    const e = {
      mass: 0,
      area: 0,
      inertia: 0,
      centre: {
        x: 0,
        y: 0
      }
    };
    for (let s = t.parts.length === 1 ? 0 : 1; s < t.parts.length; s++) {
      const i = t.parts[s], n = i.mass !== 1 / 0 ? i.mass : 1;
      e.mass += n, e.area += i.area, e.inertia += i.inertia, e.centre = v.add(e.centre, v.mult(i.position, n));
    }
    return e.centre = v.div(e.centre, e.mass), e;
  }
}, qt = class Ct {
  static rectangle(t, e, s, i, n = {}) {
    const a = {
      label: "Rectangle Body",
      position: {
        x: t,
        y: e
      },
      vertices: C.fromPath(`L 0 0 L ${s} 0 L ${s} ${i} L 0 ${i}`)
    };
    if (n.chamfer) {
      const o = n.chamfer;
      a.vertices = C.chamfer(a.vertices, o.radius, o.quality, o.qualityMin, o.qualityMax), delete n.chamfer;
    }
    return q.create(m.extend({}, a, n));
  }
  static trapezoid(t, e, s, i, n, a = {}) {
    n >= 1 && m.warn("Bodies.trapezoid: slope parameter must be < 1."), n *= 0.5;
    const o = (1 - n * 2) * s, r = s * n, l = r + o, c = l + r;
    let f;
    n < 0.5 ? f = `L 0 0 L ${r} ${-i} L ${l} ${-i} L ${c} 0` : f = `L 0 0 L ${l} ${-i} L ${c} 0`;
    const p = {
      label: "Trapezoid Body",
      position: {
        x: t,
        y: e
      },
      vertices: C.fromPath(f)
    };
    if (a.chamfer) {
      const u = a.chamfer;
      p.vertices = C.chamfer(p.vertices, u.radius, u.quality, u.qualityMin, u.qualityMax), delete a.chamfer;
    }
    return q.create(m.extend({}, p, a));
  }
  static circle(t, e, s, i = {}, n) {
    const a = {
      label: "Circle Body",
      circleRadius: s
    };
    n = n || 25;
    let o = Math.ceil(Math.max(10, Math.min(n, s)));
    return o % 2 === 1 && (o += 1), Ct.polygon(t, e, o, s, m.extend({}, a, i));
  }
  static polygon(t, e, s, i, n = {}) {
    if (s < 3) return Ct.circle(t, e, i, n);
    const a = 2 * Math.PI / s, o = a * 0.5;
    let r = "";
    for (let c = 0; c < s; c += 1) {
      const f = o + c * a, p = Math.cos(f) * i, u = Math.sin(f) * i;
      r += `L ${p.toFixed(3)} ${u.toFixed(3)} `;
    }
    const l = {
      label: "Polygon Body",
      position: {
        x: t,
        y: e
      },
      vertices: C.fromPath(r)
    };
    if (n.chamfer) {
      const c = n.chamfer;
      l.vertices = C.chamfer(l.vertices, c.radius, c.quality, c.qualityMin, c.qualityMax), delete n.chamfer;
    }
    return q.create(m.extend({}, l, n));
  }
  static fromVertices(t, e, s, i = {}, n = !1, a = 0.01, o = 10, r = 0.01) {
    const l = m.getDecomp();
    let c, f, p, u, g, y, h, d, x, S, P;
    for (c = !!(l && l.quickDecomp), p = [], Array.isArray(s[0]) || (s = [s]), S = 0; S < s.length; S += 1)
      if (y = s[S], u = C.isConvex(y), g = !u, g && !c && m.warnOnce("Bodies.fromVertices: Install the 'poly-decomp' library and use Common.setDecomp or provide 'decomp' as a global to decompose concave vertices."), u || !c)
        u ? y = C.clockwiseSort(y) : y = C.hull(y), p.push({
          position: {
            x: t,
            y: e
          },
          vertices: y
        });
      else {
        const A = y.map((I) => [I.x, I.y]);
        l.makeCCW(A), a !== !1 && l.removeCollinearPoints(A, a), r !== !1 && l.removeDuplicatePoints && l.removeDuplicatePoints(A, r);
        const k = l.quickDecomp(A);
        for (h = 0; h < k.length; h++) {
          const I = k[h].map((F) => ({
            x: F[0],
            y: F[1]
          }));
          o > 0 && C.area(I) < o || p.push({
            position: C.centre(I),
            vertices: I
          });
        }
      }
    for (h = 0; h < p.length; h++) p[h] = q.create(m.extend(p[h], i));
    if (n)
      for (h = 0; h < p.length; h++) {
        const k = p[h];
        for (d = h + 1; d < p.length; d++) {
          const I = p[d];
          if (H.overlaps(k.bounds, I.bounds)) {
            const F = k.vertices, L = I.vertices;
            for (x = 0; x < k.vertices.length; x++) for (P = 0; P < I.vertices.length; P++) {
              const W = v.magnitudeSquared(v.sub(F[(x + 1) % F.length], L[P])), O = v.magnitudeSquared(v.sub(F[x], L[(P + 1) % L.length]));
              W < 5 && O < 5 && (F[x].isInternal = !0, L[P].isInternal = !0);
            }
          }
        }
      }
    return p.length > 1 ? (f = q.create(m.extend({ parts: p.slice(0) }, i)), q.setPosition(f, {
      x: t,
      y: e
    }), f) : p[0];
  }
}, It = class Wt {
  constructor(t) {
    this.vertex = t, this.normalImpulse = 0, this.tangentImpulse = 0;
  }
  static create(t) {
    return new Wt(t);
  }
}, rt = class mt {
  constructor(t, e) {
    const s = t.bodyA, i = t.bodyB;
    this.id = mt.id(s, i), this.bodyA = s, this.bodyB = i, this.collision = t, this.contacts = [It.create(), It.create()], this.contactCount = 0, this.separation = 0, this.isActive = !0, this.isSensor = s.isSensor || i.isSensor, this.timeCreated = e, this.timeUpdated = e, this.inverseMass = 0, this.friction = 0, this.frictionStatic = 0, this.restitution = 0, this.slop = 0, mt.update(this, t, e);
  }
  static create(t, e) {
    return new mt(t, e);
  }
  static update(t, e, s) {
    const i = e.supports, n = e.supportCount, a = t.contacts, o = e.parentA, r = e.parentB;
    t.isActive = !0, t.timeUpdated = s, t.collision = e, t.separation = e.depth, t.inverseMass = o.inverseMass + r.inverseMass, t.friction = o.friction < r.friction ? o.friction : r.friction, t.frictionStatic = o.frictionStatic > r.frictionStatic ? o.frictionStatic : r.frictionStatic, t.restitution = o.restitution > r.restitution ? o.restitution : r.restitution, t.slop = o.slop > r.slop ? o.slop : r.slop, t.contactCount = n, e.pair = t;
    const l = i[0], c = i[1];
    let f = a[0], p = a[1];
    (p.vertex === l || f.vertex === c) && (a[1] = f, a[0] = f = p, p = a[1]), f.vertex = l, p.vertex = c;
  }
  static setActive(t, e, s) {
    e ? (t.isActive = !0, t.timeUpdated = s) : (t.isActive = !1, t.contactCount = 0);
  }
  static id(t, e) {
    return t.id < e.id ? `${t.id.toString(36)}:${e.id.toString(36)}` : `${e.id.toString(36)}:${t.id.toString(36)}`;
  }
}, et = [], ut = {
  overlap: 0,
  axis: null
}, gt = {
  overlap: 0,
  axis: null
}, vt = class J {
  constructor(t, e) {
    this.pair = null, this.collided = !1, this.bodyA = t, this.bodyB = e, this.parentA = t.parent, this.parentB = e.parent, this.depth = 0, this.normal = {
      x: 0,
      y: 0
    }, this.tangent = {
      x: 0,
      y: 0
    }, this.penetration = {
      x: 0,
      y: 0
    }, this.supports = [null, null], this.supportCount = 0;
  }
  static create(t, e) {
    return new J(t, e);
  }
  static collides(t, e, s) {
    if (J._overlapAxes(ut, t.vertices, e.vertices, t.axes), ut.overlap <= 0 || (J._overlapAxes(gt, e.vertices, t.vertices, e.axes), gt.overlap <= 0)) return null;
    const i = s && s.table[rt.id(t, e)];
    let n;
    i ? n = i.collision : (n = J.create(t, e), n.collided = !0, n.bodyA = t.id < e.id ? t : e, n.bodyB = t.id < e.id ? e : t, n.parentA = n.bodyA.parent, n.parentB = n.bodyB.parent), t = n.bodyA, e = n.bodyB;
    let a;
    ut.overlap < gt.overlap ? a = ut : a = gt;
    const o = n.normal, r = n.tangent, l = n.penetration, c = n.supports, f = a.overlap, p = a.axis, u = e.position.x - t.position.x, g = e.position.y - t.position.y;
    let y = p.x, h = p.y;
    y * u + h * g >= 0 && (y = -y, h = -h), o.x = y, o.y = h, r.x = -h, r.y = y, l.x = y * f, l.y = h * f, n.depth = f;
    const d = J._findSupports(t, e, o, 1);
    let x = 0;
    if (C.contains(t.vertices, d[0]) && (c[x++] = d[0]), C.contains(t.vertices, d[1]) && (c[x++] = d[1]), x < 2) {
      const S = J._findSupports(e, t, o, -1);
      C.contains(e.vertices, S[0]) && (c[x++] = S[0]), x < 2 && C.contains(e.vertices, S[1]) && (c[x++] = S[1]);
    }
    return x === 0 && (c[x++] = d[0]), n.supportCount = x, n;
  }
  static _overlapAxes(t, e, s, i) {
    const n = e.length, a = s.length, o = e[0].x, r = e[0].y, l = s[0].x, c = s[0].y, f = i.length;
    let p = Number.MAX_VALUE, u = 0, g, y, h, d, x, S;
    for (x = 0; x < f; x++) {
      const P = i[x], A = P.x, k = P.y;
      let I = o * A + r * k, F = l * A + c * k, L = I, W = F;
      for (S = 1; S < n; S += 1)
        d = e[S].x * A + e[S].y * k, d > L ? L = d : d < I && (I = d);
      for (S = 1; S < a; S += 1)
        d = s[S].x * A + s[S].y * k, d > W ? W = d : d < F && (F = d);
      if (y = L - F, h = W - I, g = y < h ? y : h, g < p && (p = g, u = x, g <= 0))
        break;
    }
    t.axis = i[u], t.overlap = p;
  }
  static _findSupports(t, e, s, i) {
    const n = e.vertices, a = n.length, o = t.position.x, r = t.position.y, l = s.x * i, c = s.y * i;
    let f = n[0], p = f, u = l * (o - p.x) + c * (r - p.y), g, y, h;
    for (h = 1; h < a; h += 1)
      p = n[h], y = l * (o - p.x) + c * (r - p.y), y < u && (u = y, f = p);
    return g = n[(a + f.index - 1) % a], u = l * (o - g.x) + c * (r - g.y), p = n[(f.index + 1) % a], l * (o - p.x) + c * (r - p.y) < u ? (et[0] = f, et[1] = p, et) : (et[0] = f, et[1] = g, et);
  }
}, R = class M {
  constructor(t) {
    const e = {
      id: m.nextId(),
      type: "composite",
      parent: null,
      isModified: !1,
      bodies: [],
      constraints: [],
      composites: [],
      label: "Composite",
      plugin: {},
      cache: {
        allBodies: null,
        allConstraints: null,
        allComposites: null
      }
    };
    Object.assign(this, m.extend(e, t));
  }
  static create(t) {
    return new M(t);
  }
  static setModified(t, e, s, i) {
    if (t.isModified = e, e && t.cache && (t.cache.allBodies = null, t.cache.allConstraints = null, t.cache.allComposites = null), s && t.parent && M.setModified(t.parent, e, s, i), i) for (let n = 0; n < t.composites.length; n++) {
      const a = t.composites[n];
      M.setModified(a, e, s, i);
    }
  }
  static add(t, e) {
    const s = [].concat(e);
    E.trigger(t, "beforeAdd", { object: e });
    for (let i = 0; i < s.length; i++) {
      const n = s[i];
      switch (n.type) {
        case "body":
          if (n.parent !== n) {
            m.warn("Composite.add: skipped adding a compound body part (you must add its parent instead)");
            break;
          }
          M.addBody(t, n);
          break;
        case "constraint":
          M.addConstraint(t, n);
          break;
        case "composite":
          M.addComposite(t, n);
          break;
        case "mouseConstraint":
          M.addConstraint(t, n.constraint);
          break;
      }
    }
    return E.trigger(t, "afterAdd", { object: e }), t;
  }
  static remove(t, e, s) {
    const i = [].concat(e);
    E.trigger(t, "beforeRemove", { object: e });
    for (let n = 0; n < i.length; n++) {
      const a = i[n];
      switch (a.type) {
        case "body":
          M.removeBody(t, a, s);
          break;
        case "constraint":
          M.removeConstraint(t, a, s);
          break;
        case "composite":
          M.removeComposite(t, a, s);
          break;
        case "mouseConstraint":
          M.removeConstraint(t, a.constraint);
          break;
      }
    }
    return E.trigger(t, "afterRemove", { object: e }), t;
  }
  static addComposite(t, e) {
    return t.composites.push(e), e.parent = t, M.setModified(t, !0, !0, !1), t;
  }
  static removeComposite(t, e, s) {
    const i = t.composites.indexOf(e);
    if (i !== -1) {
      const n = M.allBodies(e);
      M.removeCompositeAt(t, i);
      for (let a = 0; a < n.length; a++) n[a].sleepCounter = 0;
    }
    if (s) for (let n = 0; n < t.composites.length; n++) M.removeComposite(t.composites[n], e, !0);
    return t;
  }
  static removeCompositeAt(t, e) {
    return t.composites.splice(e, 1), M.setModified(t, !0, !0, !1), t;
  }
  static addBody(t, e) {
    return t.bodies.push(e), M.setModified(t, !0, !0, !1), t;
  }
  static removeBody(t, e, s) {
    const i = t.bodies.indexOf(e);
    if (i !== -1 && (M.removeBodyAt(t, i), e.sleepCounter = 0), s) for (let n = 0; n < t.composites.length; n++) M.removeBody(t.composites[n], e, !0);
    return t;
  }
  static removeBodyAt(t, e) {
    return t.bodies.splice(e, 1), M.setModified(t, !0, !0, !1), t;
  }
  static addConstraint(t, e) {
    return t.constraints.push(e), M.setModified(t, !0, !0, !1), t;
  }
  static removeConstraint(t, e, s) {
    const i = t.constraints.indexOf(e);
    if (i !== -1 && M.removeConstraintAt(t, i), s) for (let n = 0; n < t.composites.length; n++) M.removeConstraint(t.composites[n], e, !0);
    return t;
  }
  static removeConstraintAt(t, e) {
    return t.constraints.splice(e, 1), M.setModified(t, !0, !0, !1), t;
  }
  static clear(t, e, s) {
    if (s) for (let i = 0; i < t.composites.length; i++) M.clear(t.composites[i], e, !0);
    return e ? t.bodies = t.bodies.filter((i) => i.isStatic) : t.bodies.length = 0, t.constraints.length = 0, t.composites.length = 0, M.setModified(t, !0, !0, !1), t;
  }
  static allBodies(t) {
    if (t.cache && t.cache.allBodies) return t.cache.allBodies;
    let e = [].concat(t.bodies);
    for (let s = 0; s < t.composites.length; s++) e = e.concat(M.allBodies(t.composites[s]));
    return t.cache && (t.cache.allBodies = e), e;
  }
  static allConstraints(t) {
    if (t.cache && t.cache.allConstraints) return t.cache.allConstraints;
    let e = [].concat(t.constraints);
    for (let s = 0; s < t.composites.length; s++) e = e.concat(M.allConstraints(t.composites[s]));
    return t.cache && (t.cache.allConstraints = e), e;
  }
  static allComposites(t) {
    if (t.cache && t.cache.allComposites) return t.cache.allComposites;
    let e = [].concat(t.composites);
    for (let s = 0; s < t.composites.length; s++) e = e.concat(M.allComposites(t.composites[s]));
    return t.cache && (t.cache.allComposites = e), e;
  }
  static get(t, e, s) {
    let i, n;
    switch (s) {
      case "body":
        i = M.allBodies(t);
        break;
      case "constraint":
        i = M.allConstraints(t);
        break;
      case "composite":
        i = M.allComposites(t).concat(t);
        break;
    }
    return i ? (n = i.filter((a) => a.id.toString() === e.toString()), n.length === 0 ? null : n[0]) : null;
  }
  static move(t, e, s) {
    return M.remove(t, e), M.add(s, e), t;
  }
  static rebase(t) {
    const e = M.allBodies(t).concat(M.allConstraints(t)).concat(M.allComposites(t));
    for (let s = 0; s < e.length; s++) e[s].id = m.nextId();
    return t;
  }
  static translate(t, e, s) {
    const i = s ? M.allBodies(t) : t.bodies;
    for (let n = 0; n < i.length; n++) q.translate(i[n], e);
    return t;
  }
  static rotate(t, e, s, i) {
    const n = Math.cos(e), a = Math.sin(e), o = i ? M.allBodies(t) : t.bodies;
    for (let r = 0; r < o.length; r++) {
      const l = o[r], c = l.position.x - s.x, f = l.position.y - s.y;
      q.setPosition(l, {
        x: s.x + (c * n - f * a),
        y: s.y + (c * a + f * n)
      }), q.rotate(l, e);
    }
    return t;
  }
  static scale(t, e, s, i, n) {
    const a = n ? M.allBodies(t) : t.bodies;
    for (let o = 0; o < a.length; o++) {
      const r = a[o], l = r.position.x - i.x, c = r.position.y - i.y;
      q.setPosition(r, {
        x: i.x + l * e,
        y: i.y + c * s
      }), q.scale(r, e, s);
    }
    return t;
  }
  static bounds(t) {
    const e = M.allBodies(t), s = [];
    for (let i = 0; i < e.length; i += 1) {
      const n = e[i];
      s.push(n.bounds.min, n.bounds.max);
    }
    return H.create(s);
  }
}, X = class Y {
  static _warming = 0.4;
  static _torqueDampen = 1;
  static _minLength = 1e-6;
  constructor(t = {}) {
    Object.assign(this, t), this.bodyA && !this.pointA && (this.pointA = {
      x: 0,
      y: 0
    }), this.bodyB && !this.pointB && (this.pointB = {
      x: 0,
      y: 0
    });
    const e = this.bodyA ? v.add(this.bodyA.position, this.pointA) : this.pointA, s = this.bodyB ? v.add(this.bodyB.position, this.pointB) : this.pointB, i = v.magnitude(v.sub(e, s));
    this.length = typeof this.length < "u" ? this.length : i, this.id = this.id || m.nextId(), this.label = this.label || "Constraint", this.type = "constraint", this.stiffness = this.stiffness || (this.length > 0 ? 1 : 0.7), this.damping = this.damping || 0, this.angularStiffness = this.angularStiffness || 0, this.angleA = this.bodyA ? this.bodyA.angle : this.angleA, this.angleB = this.bodyB ? this.bodyB.angle : this.angleB, this.plugin = {};
    const n = {
      visible: !0,
      lineWidth: 2,
      strokeStyle: "#ffffff",
      type: "line",
      anchors: !0
    };
    this.length === 0 && this.stiffness > 0.1 ? (n.type = "pin", n.anchors = !1) : this.stiffness < 0.9 && (n.type = "spring"), this.render = m.extend(n, this.render);
  }
  static create(t) {
    return new Y(t);
  }
  static preSolveAll(t) {
    for (let e = 0; e < t.length; e += 1) {
      const s = t[e], i = s.constraintImpulse;
      s.isStatic || i.x === 0 && i.y === 0 && i.angle === 0 || (s.position.x += i.x, s.position.y += i.y, s.angle += i.angle);
    }
  }
  static solveAll(t, e) {
    const s = m.clamp(e / m._baseDelta, 0, 1);
    for (let i = 0; i < t.length; i += 1) {
      const n = t[i], a = !n.bodyA || n.bodyA && n.bodyA.isStatic, o = !n.bodyB || n.bodyB && n.bodyB.isStatic;
      (a || o) && Y.solve(t[i], s);
    }
    for (let i = 0; i < t.length; i += 1) {
      const n = t[i], a = !n.bodyA || n.bodyA && n.bodyA.isStatic, o = !n.bodyB || n.bodyB && n.bodyB.isStatic;
      !a && !o && Y.solve(t[i], s);
    }
  }
  static solve(t, e) {
    const s = t.bodyA, i = t.bodyB, n = t.pointA, a = t.pointB;
    if (!s && !i) return;
    s && !s.isStatic && (v.rotate(n, s.angle - t.angleA, n), t.angleA = s.angle), i && !i.isStatic && (v.rotate(a, i.angle - t.angleB, a), t.angleB = i.angle);
    let o = n, r = a;
    if (s && (o = v.add(s.position, n)), i && (r = v.add(i.position, a)), !o || !r) return;
    const l = v.sub(o, r);
    let c = v.magnitude(l);
    c < Y._minLength && (c = Y._minLength);
    const f = (c - t.length) / c, p = t.stiffness >= 1 || t.length === 0 ? t.stiffness * e : t.stiffness * e * e, u = t.damping * e, g = v.mult(l, f * p), y = (s ? s.inverseMass : 0) + (i ? i.inverseMass : 0), h = y + ((s ? s.inverseInertia : 0) + (i ? i.inverseInertia : 0));
    let d, x, S, P, A;
    if (u > 0) {
      const k = v.create();
      S = v.div(l, c), A = v.sub(i && v.sub(i.position, i.positionPrev) || k, s && v.sub(s.position, s.positionPrev) || k), P = v.dot(S, A);
    }
    s && !s.isStatic && (x = s.inverseMass / y, s.constraintImpulse.x -= g.x * x, s.constraintImpulse.y -= g.y * x, s.position.x -= g.x * x, s.position.y -= g.y * x, u > 0 && (s.positionPrev.x -= u * S.x * P * x, s.positionPrev.y -= u * S.y * P * x), d = v.cross(n, g) / h * Y._torqueDampen * s.inverseInertia * (1 - t.angularStiffness), s.constraintImpulse.angle -= d, s.angle -= d), i && !i.isStatic && (x = i.inverseMass / y, i.constraintImpulse.x += g.x * x, i.constraintImpulse.y += g.y * x, i.position.x += g.x * x, i.position.y += g.y * x, u > 0 && (i.positionPrev.x += u * S.x * P * x, i.positionPrev.y += u * S.y * P * x), d = v.cross(a, g) / h * Y._torqueDampen * i.inverseInertia * (1 - t.angularStiffness), i.constraintImpulse.angle += d, i.angle += d);
  }
  static postSolveAll(t) {
    for (let e = 0; e < t.length; e++) {
      const s = t[e], i = s.constraintImpulse;
      if (!(s.isStatic || i.x === 0 && i.y === 0 && i.angle === 0)) {
        Z.set(s, !1);
        for (let n = 0; n < s.parts.length; n++) {
          const a = s.parts[n];
          C.translate(a.vertices, i), n > 0 && (a.position.x += i.x, a.position.y += i.y), i.angle !== 0 && (C.rotate(a.vertices, i.angle, s.position), _.rotate(a.axes, i.angle), n > 0 && v.rotateAbout(a.position, i.angle, s.position, a.position)), H.update(a.bounds, a.vertices, s.velocity);
        }
        i.angle *= Y._warming, i.x *= Y._warming, i.y *= Y._warming;
      }
    }
  }
  static pointAWorld(t) {
    return {
      x: (t.bodyA ? t.bodyA.position.x : 0) + (t.pointA ? t.pointA.x : 0),
      y: (t.bodyA ? t.bodyA.position.y : 0) + (t.pointA ? t.pointA.y : 0)
    };
  }
  static pointBWorld(t) {
    return {
      x: (t.bodyB ? t.bodyB.position.x : 0) + (t.pointB ? t.pointB.x : 0),
      y: (t.bodyB ? t.bodyB.position.y : 0) + (t.pointB ? t.pointB.y : 0)
    };
  }
  static currentLength(t) {
    const e = (t.bodyA ? t.bodyA.position.x : 0) + (t.pointA ? t.pointA.x : 0), s = (t.bodyA ? t.bodyA.position.y : 0) + (t.pointA ? t.pointA.y : 0), i = (t.bodyB ? t.bodyB.position.x : 0) + (t.pointB ? t.pointB.x : 0), n = (t.bodyB ? t.bodyB.position.y : 0) + (t.pointB ? t.pointB.y : 0), a = e - i, o = s - n;
    return Math.sqrt(a * a + o * o);
  }
}, ee = class Ut {
  static stack(t, e, s, i, n, a, o) {
    const r = R.create({ label: "Stack" });
    let l = t, c = e, f, p = 0;
    for (let u = 0; u < i; u++) {
      let g = 0;
      for (let y = 0; y < s; y++) {
        const h = o(l, c, y, u, f, p);
        if (h) {
          const d = h.bounds.max.y - h.bounds.min.y, x = h.bounds.max.x - h.bounds.min.x;
          d > g && (g = d), q.translate(h, {
            x: x * 0.5,
            y: d * 0.5
          }), l = h.bounds.max.x + n, R.addBody(r, h), f = h, p += 1;
        } else l += n;
      }
      c += g + a, l = t;
    }
    return r;
  }
  static chain(t, e, s, i, n, a) {
    const o = t.bodies;
    for (let r = 1; r < o.length; r++) {
      const l = o[r - 1], c = o[r], f = l.bounds.max.y - l.bounds.min.y, p = l.bounds.max.x - l.bounds.min.x, u = c.bounds.max.y - c.bounds.min.y, g = c.bounds.max.x - c.bounds.min.x, y = {
        bodyA: l,
        pointA: {
          x: p * e,
          y: f * s
        },
        bodyB: c,
        pointB: {
          x: g * i,
          y: u * n
        }
      }, h = m.extend(y, a);
      R.addConstraint(t, X.create(h));
    }
    return t.label += " Chain", t;
  }
  static mesh(t, e, s, i, n) {
    const a = t.bodies;
    let o, r, l, c, f;
    for (o = 0; o < s; o++) {
      for (r = 1; r < e; r++)
        l = a[r - 1 + o * e], c = a[r + o * e], R.addConstraint(t, X.create(m.extend({
          bodyA: l,
          bodyB: c
        }, n)));
      if (o > 0) for (r = 0; r < e; r++)
        l = a[r + (o - 1) * e], c = a[r + o * e], R.addConstraint(t, X.create(m.extend({
          bodyA: l,
          bodyB: c
        }, n))), i && r > 0 && (f = a[r - 1 + (o - 1) * e], R.addConstraint(t, X.create(m.extend({
          bodyA: f,
          bodyB: c
        }, n)))), i && r < e - 1 && (f = a[r + 1 + (o - 1) * e], R.addConstraint(t, X.create(m.extend({
          bodyA: f,
          bodyB: c
        }, n))));
    }
    return t.label += " Mesh", t;
  }
  static pyramid(t, e, s, i, n, a, o) {
    return Ut.stack(t, e, s, i, n, a, (r, l, c, f, p, u) => {
      const g = Math.min(i, Math.ceil(s / 2)), y = p ? p.bounds.max.x - p.bounds.min.x : 0;
      if (f > g) return;
      f = g - f;
      const h = f, d = s - 1 - f;
      if (!(c < h || c > d))
        return u === 1 && q.translate(p, {
          x: (c + (s % 2 === 1 ? 1 : -1)) * y,
          y: 0
        }), o(t + (p ? c * y : 0) + c * n, l, c, f, p, u);
    });
  }
}, nt = class dt {
  constructor(t) {
    Object.assign(this, m.extend({
      bodies: [],
      collisions: [],
      pairs: null
    }, t));
  }
  static create(t) {
    return new dt(t);
  }
  static setBodies(t, e) {
    t.bodies = e.slice(0);
  }
  static clear(t) {
    t.bodies = [], t.collisions = [];
  }
  static collisions(t) {
    const e = t.pairs, s = t.bodies, i = s.length, n = dt.canCollide, a = vt.collides, o = t.collisions;
    let r = 0, l, c;
    for (s.sort(dt._compareBoundsX), l = 0; l < i; l++) {
      const f = s[l], p = f.bounds.max.x, u = f.bounds.max.y, g = f.bounds.min.y, y = f.isStatic || f.isSleeping, h = f.parts.length, d = h === 1;
      for (c = l + 1; c < i; c++) {
        const x = s[c], S = x.bounds;
        if (S.min.x > p) break;
        if (u < S.min.y || g > S.max.y || y && (x.isStatic || x.isSleeping) || !n(f.collisionFilter, x.collisionFilter)) continue;
        const P = x.parts.length;
        if (d && P === 1) {
          const A = a(f, x, e);
          A && (o[r++] = A);
        } else {
          const A = h > 1 ? 1 : 0, k = P > 1 ? 1 : 0;
          for (let I = A; I < h; I++) {
            const F = f.parts[I], L = F.bounds;
            for (let W = k; W < P; W++) {
              const O = x.parts[W], tt = O.bounds;
              if (L.min.x > tt.max.x || L.max.x < tt.min.x || L.max.y < tt.min.y || L.min.y > tt.max.y) continue;
              const at = a(F, O, e);
              at && (o[r++] = at);
            }
          }
        }
      }
    }
    return o.length !== r && (o.length = r), o;
  }
  static canCollide(t, e) {
    return t.group === e.group && t.group !== 0 ? t.group > 0 : (t.mask & e.category) !== 0 && (e.mask & t.category) !== 0;
  }
  static _compareBoundsX(t, e) {
    return t.bounds.min.x - e.bounds.min.x;
  }
}, it = class K {
  static _restingThresh = 2;
  static _restingThreshTangent = Math.sqrt(6);
  static _positionDampen = 0.9;
  static _positionWarming = 0.8;
  static _frictionNormalMultiplier = 5;
  static _frictionMaxStatic = Number.MAX_VALUE;
  static preSolvePosition(t) {
    let e, s, i;
    const n = t.length;
    for (e = 0; e < n; e++)
      s = t[e], s.isActive && (i = s.contactCount, s.collision.parentA.totalContacts += i, s.collision.parentB.totalContacts += i);
  }
  static solvePosition(t, e, s) {
    let i, n, a, o, r, l, c, f;
    const p = K._positionDampen * (s || 1), u = m.clamp(e / m._baseDelta, 0, 1), g = t.length;
    for (i = 0; i < g; i++)
      n = t[i], !(!n.isActive || n.isSensor) && (a = n.collision, o = a.parentA, r = a.parentB, l = a.normal, n.separation = a.depth + l.x * (r.positionImpulse.x - o.positionImpulse.x) + l.y * (r.positionImpulse.y - o.positionImpulse.y));
    for (i = 0; i < g; i++)
      n = t[i], !(!n.isActive || n.isSensor) && (a = n.collision, o = a.parentA, r = a.parentB, l = a.normal, f = n.separation - n.slop * u, (o.isStatic || r.isStatic) && (f *= 2), o.isStatic || o.isSleeping || (c = p / o.totalContacts, o.positionImpulse.x += l.x * f * c, o.positionImpulse.y += l.y * f * c), r.isStatic || r.isSleeping || (c = p / r.totalContacts, r.positionImpulse.x -= l.x * f * c, r.positionImpulse.y -= l.y * f * c));
  }
  static postSolvePosition(t) {
    const e = K._positionWarming, s = t.length, i = C.translate, n = H.update;
    for (let a = 0; a < s; a++) {
      const o = t[a], r = o.positionImpulse, l = r.x, c = r.y, f = o.velocity;
      if (o.totalContacts = 0, l !== 0 || c !== 0) {
        for (let p = 0; p < o.parts.length; p++) {
          const u = o.parts[p];
          i(u.vertices, r), n(u.bounds, u.vertices, f), u.position.x += l, u.position.y += c;
        }
        o.positionPrev.x += l, o.positionPrev.y += c, l * f.x + c * f.y < 0 ? (r.x = 0, r.y = 0) : (r.x *= e, r.y *= e);
      }
    }
  }
  static preSolveVelocity(t) {
    const e = t.length;
    let s, i;
    for (s = 0; s < e; s++) {
      const n = t[s];
      if (!n.isActive || n.isSensor) continue;
      const a = n.contacts, o = n.contactCount, r = n.collision, l = r.parentA, c = r.parentB, f = r.normal, p = r.tangent;
      for (i = 0; i < o; i++) {
        const u = a[i], g = u.vertex, y = u.normalImpulse, h = u.tangentImpulse;
        if (y !== 0 || h !== 0) {
          const d = f.x * y + p.x * h, x = f.y * y + p.y * h;
          l.isStatic || l.isSleeping || (l.positionPrev.x += d * l.inverseMass, l.positionPrev.y += x * l.inverseMass, l.anglePrev += l.inverseInertia * ((g.x - l.position.x) * x - (g.y - l.position.y) * d)), c.isStatic || c.isSleeping || (c.positionPrev.x -= d * c.inverseMass, c.positionPrev.y -= x * c.inverseMass, c.anglePrev -= c.inverseInertia * ((g.x - c.position.x) * x - (g.y - c.position.y) * d));
        }
      }
    }
  }
  static solveVelocity(t, e) {
    const s = e / m._baseDelta, i = s * s * s, n = -K._restingThresh * s, a = K._restingThreshTangent, o = K._frictionNormalMultiplier * s, r = K._frictionMaxStatic, l = t.length;
    let c, f, p, u;
    for (p = 0; p < l; p++) {
      const g = t[p];
      if (!g.isActive || g.isSensor) continue;
      const y = g.collision, h = y.parentA, d = y.parentB, x = y.normal.x, S = y.normal.y, P = y.tangent.x, A = y.tangent.y, k = g.inverseMass, I = g.friction * g.frictionStatic * o, F = g.contacts, L = g.contactCount, W = 1 / L, O = h.position.x - h.positionPrev.x, tt = h.position.y - h.positionPrev.y, at = h.angle - h.anglePrev, Qt = d.position.x - d.positionPrev.x, Zt = d.position.y - d.positionPrev.y, kt = d.angle - d.anglePrev;
      for (u = 0; u < L; u++) {
        const U = F[u], lt = U.vertex, St = lt.x - h.position.x, wt = lt.y - h.position.y, Pt = lt.x - d.position.x, bt = lt.y - d.position.y, Jt = O - wt * at, Kt = tt + St * at, _t = Qt - bt * kt, te = Zt + Pt * kt, Tt = Jt - _t, Vt = Kt - te, At = x * Tt + S * Vt, j = P * Tt + A * Vt, Dt = g.separation + At;
        let Bt = Math.min(Dt, 1);
        Bt = Dt < 0 ? 0 : Bt;
        const Lt = Bt * I;
        j < -Lt || j > Lt ? (f = j > 0 ? j : -j, c = g.friction * (j > 0 ? 1 : -1) * i, c < -f ? c = -f : c > f && (c = f)) : (c = j, f = r);
        const Rt = St * S - wt * x, Et = Pt * S - bt * x, Ft = W / (k + h.inverseInertia * Rt * Rt + d.inverseInertia * Et * Et);
        let ct = (1 + g.restitution) * At * Ft;
        if (c *= Ft, At < n) U.normalImpulse = 0;
        else {
          const Mt = U.normalImpulse;
          U.normalImpulse += ct, U.normalImpulse > 0 && (U.normalImpulse = 0), ct = U.normalImpulse - Mt;
        }
        if (j < -a || j > a) U.tangentImpulse = 0;
        else {
          const Mt = U.tangentImpulse;
          U.tangentImpulse += c, U.tangentImpulse < -f && (U.tangentImpulse = -f), U.tangentImpulse > f && (U.tangentImpulse = f), c = U.tangentImpulse - Mt;
        }
        const ft = x * ct + P * c, pt = S * ct + A * c;
        h.isStatic || h.isSleeping || (h.positionPrev.x += ft * h.inverseMass, h.positionPrev.y += pt * h.inverseMass, h.anglePrev += (St * pt - wt * ft) * h.inverseInertia), d.isStatic || d.isSleeping || (d.positionPrev.x -= ft * d.inverseMass, d.positionPrev.y -= pt * d.inverseMass, d.anglePrev -= (Pt * pt - bt * ft) * d.inverseInertia);
      }
    }
  }
}, xt = class $t {
  constructor(t) {
    Object.assign(this, m.extend({
      table: {},
      list: [],
      collisionStart: [],
      collisionActive: [],
      collisionEnd: []
    }, t));
  }
  static create(t) {
    return new $t(t);
  }
  static update(t, e, s) {
    const i = rt.update, n = rt.create, a = rt.setActive, o = t.table, r = t.list, l = t.collisionStart, c = t.collisionEnd, f = t.collisionActive, p = e.length;
    let u = r.length, g = u, y = 0, h = 0, d = 0, x, S, P;
    for (P = 0; P < p; P++)
      x = e[P], S = x.pair, S ? (S.isActive && (f[d++] = S), i(S, x, s)) : (S = n(x, s), o[S.id] = S, l[y++] = S, r[g++] = S);
    for (g = 0, u = r.length, P = 0; P < u; P++)
      S = r[P], S.timeUpdated >= s ? r[g++] = S : (a(S, !1, s), S.collision.bodyA.sleepCounter > 0 && S.collision.bodyB.sleepCounter > 0 ? r[g++] = S : (c[h++] = S, delete o[S.id]));
    r.length !== g && (r.length = g), l.length !== y && (l.length = y), c.length !== h && (c.length = h), f.length !== d && (f.length = d);
  }
  static clear(t) {
    return t.table = {}, t.list.length = 0, t.collisionStart.length = 0, t.collisionActive.length = 0, t.collisionEnd.length = 0, t;
  }
}, Xt = class N {
  static _deltaMax = 1e3 / 60;
  constructor(t = {}) {
    Object.assign(this, m.extend({
      positionIterations: 6,
      velocityIterations: 4,
      constraintIterations: 2,
      enableSleeping: !1,
      events: [],
      plugin: {},
      gravity: {
        x: 0,
        y: 1,
        scale: 1e-3
      },
      timing: {
        timestamp: 0,
        timeScale: 1,
        lastDelta: 0,
        lastElapsed: 0,
        lastUpdatesPerFrame: 0
      }
    }, t)), this.world = t.world || R.create({ label: "World" }), this.pairs = t.pairs || xt.create(), this.detector = t.detector || nt.create(), this.detector.pairs = this.pairs, this.world.gravity = this.gravity;
  }
  static create(t = {}) {
    return new N(t);
  }
  static update(t, e) {
    const s = m.now(), i = t.world, n = t.detector, a = t.pairs, o = t.timing, r = o.timestamp;
    let l;
    e > N._deltaMax && m.warnOnce("Matter.Engine.update: delta argument is recommended to be less than or equal to", N._deltaMax.toFixed(3), "ms."), e = e ?? m._baseDelta, e *= o.timeScale, o.timestamp += e, o.lastDelta = e;
    const c = {
      timestamp: o.timestamp,
      delta: e
    };
    E.trigger(t, "beforeUpdate", c);
    const f = R.allBodies(i), p = R.allConstraints(i);
    for (i.isModified && (nt.setBodies(n, f), R.setModified(i, !1, !1, !0)), t.enableSleeping && Z.update(f, e), N._bodiesApplyGravity(f, t.gravity), e > 0 && N._bodiesUpdate(f, e), E.trigger(t, "beforeSolve", c), X.preSolveAll(f), l = 0; l < t.constraintIterations; l++) X.solveAll(p, e);
    X.postSolveAll(f);
    const u = nt.collisions(n);
    xt.update(a, u, r), t.enableSleeping && Z.afterCollisions(a.list), a.collisionStart.length > 0 && E.trigger(t, "collisionStart", {
      pairs: a.collisionStart,
      timestamp: o.timestamp,
      delta: e
    });
    const g = m.clamp(20 / t.positionIterations, 0, 1);
    for (it.preSolvePosition(a.list), l = 0; l < t.positionIterations; l++) it.solvePosition(a.list, e, g);
    for (it.postSolvePosition(f), X.preSolveAll(f), l = 0; l < t.constraintIterations; l++) X.solveAll(p, e);
    for (X.postSolveAll(f), it.preSolveVelocity(a.list), l = 0; l < t.velocityIterations; l++) it.solveVelocity(a.list, e);
    return N._bodiesUpdateVelocities(f), a.collisionActive.length > 0 && E.trigger(t, "collisionActive", {
      pairs: a.collisionActive,
      timestamp: o.timestamp,
      delta: e
    }), a.collisionEnd.length > 0 && E.trigger(t, "collisionEnd", {
      pairs: a.collisionEnd,
      timestamp: o.timestamp,
      delta: e
    }), N._bodiesClearForces(f), E.trigger(t, "afterUpdate", c), t.timing.lastElapsed = m.now() - s, t;
  }
  static merge(t, e) {
    if (m.extend(t, e), e.world) {
      t.world = e.world, N.clear(t);
      const s = R.allBodies(t.world);
      for (let i = 0; i < s.length; i++) {
        const n = s[i];
        Z.set(n, !1), n.id = m.nextId();
      }
    }
  }
  static clear(t) {
    xt.clear(t.pairs), nt.clear(t.detector);
  }
  static _bodiesClearForces(t) {
    const e = t.length;
    for (let s = 0; s < e; s++) {
      const i = t[s];
      i.force.x = 0, i.force.y = 0, i.torque = 0;
    }
  }
  static _bodiesApplyGravity(t, e) {
    const s = typeof e.scale < "u" ? e.scale : 1e-3, i = t.length;
    if (!(e.x === 0 && e.y === 0 || s === 0))
      for (let n = 0; n < i; n++) {
        const a = t[n];
        a.isStatic || a.isSleeping || (a.force.y += a.mass * e.y * s, a.force.x += a.mass * e.x * s);
      }
  }
  static _bodiesUpdate(t, e) {
    const s = t.length;
    for (let i = 0; i < s; i++) {
      const n = t[i];
      n.isStatic || n.isSleeping || q.update(n, e);
    }
  }
  static _bodiesUpdateVelocities(t) {
    const e = t.length;
    for (let s = 0; s < e; s++) q.updateVelocities(t[s]);
  }
}, z = class st {
  constructor(t) {
    t || m.log("Mouse.create: element was undefined, defaulting to document.body", "warn"), this.element = t || document.body, this.absolute = {
      x: 0,
      y: 0
    }, this.position = {
      x: 0,
      y: 0
    }, this.mousedownPosition = {
      x: 0,
      y: 0
    }, this.mouseupPosition = {
      x: 0,
      y: 0
    }, this.offset = {
      x: 0,
      y: 0
    }, this.scale = {
      x: 1,
      y: 1
    }, this.wheelDelta = 0, this.button = -1, this.pixelRatio = parseInt(this.element.getAttribute("data-pixel-ratio"), 10) || 1, this.sourceEvents = {
      mousemove: null,
      mousedown: null,
      mouseup: null,
      mousewheel: null
    };
    const e = this;
    this.mousemove = function(s) {
      const i = st._getRelativeMousePosition(s, e.element, e.pixelRatio);
      s.changedTouches && (e.button = 0, s.preventDefault()), e.absolute.x = i.x, e.absolute.y = i.y, e.position.x = e.absolute.x * e.scale.x + e.offset.x, e.position.y = e.absolute.y * e.scale.y + e.offset.y, e.sourceEvents.mousemove = s;
    }, this.mousedown = function(s) {
      const i = st._getRelativeMousePosition(s, e.element, e.pixelRatio);
      s.changedTouches ? (e.button = 0, s.preventDefault()) : e.button = s.button, e.absolute.x = i.x, e.absolute.y = i.y, e.position.x = e.absolute.x * e.scale.x + e.offset.x, e.position.y = e.absolute.y * e.scale.y + e.offset.y, e.mousedownPosition.x = e.position.x, e.mousedownPosition.y = e.position.y, e.sourceEvents.mousedown = s;
    }, this.mouseup = function(s) {
      const i = st._getRelativeMousePosition(s, e.element, e.pixelRatio);
      s.changedTouches && s.preventDefault(), e.button = -1, e.absolute.x = i.x, e.absolute.y = i.y, e.position.x = e.absolute.x * e.scale.x + e.offset.x, e.position.y = e.absolute.y * e.scale.y + e.offset.y, e.mouseupPosition.x = e.position.x, e.mouseupPosition.y = e.position.y, e.sourceEvents.mouseup = s;
    }, this.mousewheel = function(s) {
      e.wheelDelta = Math.max(-1, Math.min(1, s.wheelDelta || -s.detail)), s.preventDefault(), e.sourceEvents.mousewheel = s;
    }, st.setElement(this, this.element);
  }
  static create(t) {
    return new st(t);
  }
  static setElement(t, e) {
    t.element = e, e.addEventListener("mousemove", t.mousemove, { passive: !0 }), e.addEventListener("mousedown", t.mousedown, { passive: !0 }), e.addEventListener("mouseup", t.mouseup, { passive: !0 }), e.addEventListener("wheel", t.mousewheel, { passive: !1 }), e.addEventListener("touchmove", t.mousemove, { passive: !1 }), e.addEventListener("touchstart", t.mousedown, { passive: !1 }), e.addEventListener("touchend", t.mouseup, { passive: !1 });
  }
  static clearSourceEvents(t) {
    t.sourceEvents.mousemove = null, t.sourceEvents.mousedown = null, t.sourceEvents.mouseup = null, t.sourceEvents.mousewheel = null, t.wheelDelta = 0;
  }
  static setOffset(t, e) {
    t.offset.x = e.x, t.offset.y = e.y, t.position.x = t.absolute.x * t.scale.x + t.offset.x, t.position.y = t.absolute.y * t.scale.y + t.offset.y;
  }
  static setScale(t, e) {
    t.scale.x = e.x, t.scale.y = e.y, t.position.x = t.absolute.x * t.scale.x + t.offset.x, t.position.y = t.absolute.y * t.scale.y + t.offset.y;
  }
  static _getRelativeMousePosition(t, e, s) {
    const i = e.getBoundingClientRect(), n = document.documentElement || document.body.parentNode || document.body, a = window.pageXOffset !== void 0 ? window.pageXOffset : n.scrollLeft, o = window.pageYOffset !== void 0 ? window.pageYOffset : n.scrollTop, r = t.changedTouches;
    let l, c;
    return r ? (l = r[0].pageX - i.left - a, c = r[0].pageY - i.top - o) : (l = t.pageX - i.left - a, c = t.pageY - i.top - o), {
      x: l / (e.clientWidth / (e.width || e.clientWidth) * s),
      y: c / (e.clientHeight / (e.height || e.clientHeight) * s)
    };
  }
}, ie = class yt {
  constructor(t, e) {
    let s = (t ? t.mouse : null) || (e ? e.mouse : null);
    s || (t && t.render && t.render.canvas ? s = z.create(t.render.canvas) : e && e.element ? s = z.create(e.element) : (s = z.create(), m.warn("MouseConstraint.create: options.mouse was undefined, options.element was undefined, may not function as expected")));
    const i = X.create({
      label: "Mouse Constraint",
      pointA: s.position,
      pointB: {
        x: 0,
        y: 0
      },
      length: 0.01,
      stiffness: 0.1,
      angularStiffness: 1,
      render: {
        strokeStyle: "#90EE90",
        lineWidth: 3
      }
    }), n = {
      type: "mouseConstraint",
      mouse: s,
      element: null,
      body: null,
      constraint: i,
      collisionFilter: {
        category: 1,
        mask: 4294967295,
        group: 0
      }
    };
    Object.assign(this, m.extend(n, e)), E.on(t, "beforeUpdate", () => {
      const a = R.allBodies(t.world);
      yt.update(this, a), yt._triggerEvents(this);
    });
  }
  static create(t, e) {
    return new yt(t, e);
  }
  static update(t, e) {
    const s = t.mouse, i = t.constraint;
    let n = t.body;
    if (s.button === 0) {
      if (i.bodyB) Z.set(i.bodyB, !1), i.pointA = s.position;
      else
        for (let a = 0; a < e.length; a++)
          if (n = e[a], H.contains(n.bounds, s.position) && nt.canCollide(n.collisionFilter, t.collisionFilter)) for (let o = n.parts.length > 1 ? 1 : 0; o < n.parts.length; o++) {
            const r = n.parts[o];
            if (C.contains(r.vertices, s.position)) {
              i.pointA = s.position, i.bodyB = t.body = n, i.pointB = {
                x: s.position.x - n.position.x,
                y: s.position.y - n.position.y
              }, i.angleB = n.angle, Z.set(n, !1), E.trigger(t, "startdrag", {
                mouse: s,
                body: n
              });
              break;
            }
          }
    } else
      i.bodyB = t.body = null, i.pointB = null, n && E.trigger(t, "enddrag", {
        mouse: s,
        body: n
      });
  }
  static _triggerEvents(t) {
    const e = t.mouse, s = e.sourceEvents;
    s.mousemove && E.trigger(t, "mousemove", { mouse: e }), s.mousedown && E.trigger(t, "mousedown", { mouse: e }), s.mouseup && E.trigger(t, "mouseup", { mouse: e }), z.clearSourceEvents(e);
  }
}, se = class Yt {
  static collides(t, e) {
    const s = [], i = e.length, n = t.bounds, a = vt.collides, o = H.overlaps;
    for (let r = 0; r < i; r++) {
      const l = e[r], c = l.parts.length, f = c === 1 ? 0 : 1;
      if (o(l.bounds, n)) for (let p = f; p < c; p++) {
        const u = l.parts[p];
        if (o(u.bounds, n)) {
          const g = a(u, t);
          if (g) {
            s.push(g);
            break;
          }
        }
      }
    }
    return s;
  }
  static ray(t, e, s, i = 1e-100) {
    const n = v.angle(e, s), a = v.magnitude(v.sub(e, s)), o = (s.x + e.x) * 0.5, r = (s.y + e.y) * 0.5, l = qt.rectangle(o, r, a, i, { angle: n }), c = Yt.collides(l, t);
    for (let f = 0; f < c.length; f += 1) {
      const p = c[f];
      p.body = p.bodyB = p.bodyA;
    }
    return c;
  }
  static region(t, e, s) {
    const i = [];
    for (let n = 0; n < t.length; n++) {
      const a = t[n], o = H.overlaps(a.bounds, e);
      (o && !s || !o && s) && i.push(a);
    }
    return i;
  }
  static point(t, e) {
    const s = [];
    for (let i = 0; i < t.length; i++) {
      const n = t[i];
      if (H.contains(n.bounds, e)) for (let a = n.parts.length === 1 ? 0 : 1; a < n.parts.length; a++) {
        const o = n.parts[a];
        if (H.contains(o.bounds, e) && C.contains(o.vertices, e)) {
          s.push(n);
          break;
        }
      }
    }
    return s;
  }
}, jt, Nt;
typeof window < "u" && (jt = window.requestAnimationFrame || window.webkitRequestAnimationFrame || window.mozRequestAnimationFrame || window.msRequestAnimationFrame || ((D) => {
  window.setTimeout(() => {
    D(m.now());
  }, 1e3 / 60);
}), Nt = window.cancelAnimationFrame || window.mozCancelAnimationFrame || window.webkitCancelAnimationFrame || window.msCancelAnimationFrame);
var ne = class b {
  static _goodFps = 30;
  static _goodDelta = 1e3 / 60;
  constructor(t = {}) {
    const e = {
      engine: null,
      element: null,
      canvas: null,
      mouse: null,
      frameRequestId: null,
      timing: {
        historySize: 60,
        delta: 0,
        deltaHistory: [],
        lastTime: 0,
        lastTimestamp: 0,
        lastElapsed: 0,
        timestampElapsed: 0,
        timestampElapsedHistory: [],
        engineDeltaHistory: [],
        engineElapsedHistory: [],
        engineUpdatesHistory: [],
        elapsedHistory: []
      },
      options: {
        width: 800,
        height: 600,
        pixelRatio: 1,
        background: "#14151f",
        wireframeBackground: "#14151f",
        wireframeStrokeStyle: "#bbb",
        hasBounds: !!t.bounds,
        enabled: !0,
        wireframes: !0,
        showSleeping: !0,
        showDebug: !1,
        showStats: !1,
        showPerformance: !1,
        showBounds: !1,
        showVelocity: !1,
        showCollisions: !1,
        showSeparations: !1,
        showAxes: !1,
        showPositions: !1,
        showAngleIndicator: !1,
        showIds: !1,
        showVertexNumbers: !1,
        showConvexHulls: !1,
        showInternalEdges: !1,
        showMousePosition: !1
      }
    };
    Object.assign(this, m.extend(e, t)), this.canvas && (this.canvas.width = this.options.width || this.canvas.width, this.canvas.height = this.options.height || this.canvas.height), this.mouse = t.mouse, this.engine = t.engine, this.canvas = this.canvas || b._createCanvas(this.options.width, this.options.height), this.context = this.canvas.getContext("2d"), this.textures = {}, this.bounds = this.bounds || {
      min: {
        x: 0,
        y: 0
      },
      max: {
        x: this.canvas.width,
        y: this.canvas.height
      }
    }, this.options.pixelRatio !== 1 && b.setPixelRatio(this, this.options.pixelRatio), m.isElement(this.element) && this.element.appendChild(this.canvas);
  }
  static create(t) {
    return new b(t);
  }
  static run(t) {
    (function e(s) {
      t.frameRequestId = jt(e), b._updateTiming(t, s), b.world(t, s), t.context.setTransform(t.options.pixelRatio, 0, 0, t.options.pixelRatio, 0, 0), (t.options.showStats || t.options.showDebug) && b.stats(t, t.context, s), (t.options.showPerformance || t.options.showDebug) && b.performance(t, t.context, s), t.context.setTransform(1, 0, 0, 1, 0, 0);
    })();
  }
  static stop(t) {
    Nt(t.frameRequestId);
  }
  static setPixelRatio(t, e) {
    const s = t.options, i = t.canvas;
    e === "auto" && (e = b._getPixelRatio(i)), s.pixelRatio = e, i.setAttribute("data-pixel-ratio", e), i.width = s.width * e, i.height = s.height * e, i.style.width = `${s.width}px`, i.style.height = `${s.height}px`;
  }
  static setSize(t, e, s) {
    t.options.width = e, t.options.height = s, t.bounds.max.x = t.bounds.min.x + e, t.bounds.max.y = t.bounds.min.y + s, t.options.pixelRatio !== 1 ? b.setPixelRatio(t, t.options.pixelRatio) : (t.canvas.width = e, t.canvas.height = s);
  }
  static lookAt(t, e, s, i = !0) {
    e = Array.isArray(e) ? e : [e], s = s || {
      x: 0,
      y: 0
    };
    const n = {
      min: {
        x: 1 / 0,
        y: 1 / 0
      },
      max: {
        x: -1 / 0,
        y: -1 / 0
      }
    };
    for (let u = 0; u < e.length; u += 1) {
      const g = e[u], y = g.bounds ? g.bounds.min : g.min || g.position || g, h = g.bounds ? g.bounds.max : g.max || g.position || g;
      y && h && (y.x < n.min.x && (n.min.x = y.x), h.x > n.max.x && (n.max.x = h.x), y.y < n.min.y && (n.min.y = y.y), h.y > n.max.y && (n.max.y = h.y));
    }
    const a = n.max.x - n.min.x + 2 * s.x, o = n.max.y - n.min.y + 2 * s.y, r = t.canvas.height, l = t.canvas.width / r, c = a / o;
    let f = 1, p = 1;
    c > l ? p = c / l : f = l / c, t.options.hasBounds = !0, t.bounds.min.x = n.min.x, t.bounds.max.x = n.min.x + a * f, t.bounds.min.y = n.min.y, t.bounds.max.y = n.min.y + o * p, i && (t.bounds.min.x += a * 0.5 - a * f * 0.5, t.bounds.max.x += a * 0.5 - a * f * 0.5, t.bounds.min.y += o * 0.5 - o * p * 0.5, t.bounds.max.y += o * 0.5 - o * p * 0.5), t.bounds.min.x -= s.x, t.bounds.max.x -= s.x, t.bounds.min.y -= s.y, t.bounds.max.y -= s.y, t.mouse && (z.setScale(t.mouse, {
      x: (t.bounds.max.x - t.bounds.min.x) / t.canvas.width,
      y: (t.bounds.max.y - t.bounds.min.y) / t.canvas.height
    }), z.setOffset(t.mouse, t.bounds.min));
  }
  static startViewTransform(t) {
    const e = t.bounds.max.x - t.bounds.min.x, s = t.bounds.max.y - t.bounds.min.y, i = e / t.options.width, n = s / t.options.height;
    t.context.setTransform(t.options.pixelRatio / i, 0, 0, t.options.pixelRatio / n, 0, 0), t.context.translate(-t.bounds.min.x, -t.bounds.min.y);
  }
  static endViewTransform(t) {
    t.context.setTransform(t.options.pixelRatio, 0, 0, t.options.pixelRatio, 0, 0);
  }
  static world(t, e) {
    const s = m.now(), i = t.engine, n = i.world, a = t.canvas, o = t.context, r = t.options, l = t.timing, c = R.allBodies(n), f = R.allConstraints(n), p = r.wireframes ? r.wireframeBackground : r.background;
    let u = [], g = [], y;
    const h = { timestamp: i.timing.timestamp };
    if (E.trigger(t, "beforeRender", h), t.currentBackground !== p && b._applyBackground(t, p), o.globalCompositeOperation = "source-in", o.fillStyle = "transparent", o.fillRect(0, 0, a.width, a.height), o.globalCompositeOperation = "source-over", r.hasBounds) {
      for (y = 0; y < c.length; y++) {
        const d = c[y];
        H.overlaps(d.bounds, t.bounds) && u.push(d);
      }
      for (y = 0; y < f.length; y++) {
        const d = f[y], x = d.bodyA, S = d.bodyB;
        let P = d.pointA, A = d.pointB;
        x && (P = v.add(x.position, d.pointA)), S && (A = v.add(S.position, d.pointB)), !(!P || !A) && (H.contains(t.bounds, P) || H.contains(t.bounds, A)) && g.push(d);
      }
      b.startViewTransform(t), t.mouse && (z.setScale(t.mouse, {
        x: (t.bounds.max.x - t.bounds.min.x) / t.options.width,
        y: (t.bounds.max.y - t.bounds.min.y) / t.options.height
      }), z.setOffset(t.mouse, t.bounds.min));
    } else
      g = f, u = c, t.options.pixelRatio !== 1 && t.context.setTransform(t.options.pixelRatio, 0, 0, t.options.pixelRatio, 0, 0);
    !r.wireframes || i.enableSleeping && r.showSleeping ? b.bodies(t, u, o) : (r.showConvexHulls && b.bodyConvexHulls(t, u, o), b.bodyWireframes(t, u, o)), r.showBounds && b.bodyBounds(t, u, o), (r.showAxes || r.showAngleIndicator) && b.bodyAxes(t, u, o), r.showPositions && b.bodyPositions(t, u, o), r.showVelocity && b.bodyVelocity(t, u, o), r.showIds && b.bodyIds(t, u, o), r.showSeparations && b.separations(t, i.pairs.list, o), r.showCollisions && b.collisions(t, i.pairs.list, o), r.showVertexNumbers && b.vertexNumbers(t, u, o), r.showMousePosition && b.mousePosition(t, t.mouse, o), b.constraints(g, o), r.hasBounds && b.endViewTransform(t), E.trigger(t, "afterRender", h), l.lastElapsed = m.now() - s;
  }
  static stats(t, e, s) {
    const i = t.engine, n = i.world, a = R.allBodies(n), o = 55, r = 44, l = 0;
    let c = 0, f = 0;
    for (let u = 0; u < a.length; u += 1) c += a[u].parts.length;
    const p = {
      Part: c,
      Body: a.length,
      Cons: R.allConstraints(n).length,
      Comp: R.allComposites(n).length,
      Pair: i.pairs.list.length
    };
    e.fillStyle = "#0e0f19", e.fillRect(f, l, o * 5.5, r), e.font = "12px Arial", e.textBaseline = "top", e.textAlign = "right";
    for (const u in p) {
      const g = p[u];
      e.fillStyle = "#aaa", e.fillText(u, f + o, l + 8), e.fillStyle = "#eee", e.fillText(g, f + o, l + 26), f += o;
    }
  }
  static performance(t, e) {
    const s = t.engine, i = t.timing, n = i.deltaHistory, a = i.elapsedHistory, o = i.timestampElapsedHistory, r = i.engineDeltaHistory, l = i.engineUpdatesHistory, c = i.engineElapsedHistory, f = s.timing.lastUpdatesPerFrame, p = s.timing.lastDelta, u = b._mean(n), g = b._mean(a), y = b._mean(r), h = b._mean(l), d = b._mean(c), x = b._mean(o) / u || 0, S = Math.round(u / p), P = 1e3 / u || 0, A = 4, k = 12, I = 60, F = 34, L = 10, W = 69;
    e.fillStyle = "#0e0f19", e.fillRect(0, 50, k * 5 + I * 6 + 22, F), b.status(e, L, W, I, A, n.length, `${Math.round(P)} fps`, P / b._goodFps, (O) => n[O] / u - 1), b.status(e, L + k + I, W, I, A, r.length, `${p.toFixed(2)} dt`, b._goodDelta / p, (O) => r[O] / y - 1), b.status(e, L + (k + I) * 2, W, I, A, l.length, `${f} upf`, Math.pow(m.clamp(h / S || 1, 0, 1), 4), (O) => l[O] / h - 1), b.status(e, L + (k + I) * 3, W, I, A, c.length, `${d.toFixed(2)} ut`, 1 - f * d / b._goodFps, (O) => c[O] / d - 1), b.status(e, L + (k + I) * 4, W, I, A, a.length, `${g.toFixed(2)} rt`, 1 - g / b._goodFps, (O) => a[O] / g - 1), b.status(e, L + (k + I) * 5, W, I, A, o.length, `${x.toFixed(2)} x`, x * x * x, (O) => (o[O] / n[O] / x || 0) - 1);
  }
  static status(t, e, s, i, n, a, o, r, l) {
    t.strokeStyle = "#888", t.fillStyle = "#444", t.lineWidth = 1, t.fillRect(e, s + 7, i, 1), t.beginPath(), t.moveTo(e, s + 7 - n * m.clamp(0.4 * l(0), -2, 2));
    for (let c = 0; c < i; c += 1) t.lineTo(e + c, s + 7 - (c < a ? n * m.clamp(0.4 * l(c), -2, 2) : 0));
    t.stroke(), t.fillStyle = `hsl(${m.clamp(25 + 95 * r, 0, 120)},100%,60%)`, t.fillRect(e, s - 7, 4, 4), t.font = "12px Arial", t.textBaseline = "middle", t.textAlign = "right", t.fillStyle = "#eee", t.fillText(o, e + i, s - 5);
  }
  static constraints(t, e) {
    const s = e;
    for (let i = 0; i < t.length; i++) {
      const n = t[i];
      if (!n.render.visible || !n.pointA || !n.pointB) continue;
      const a = n.bodyA, o = n.bodyB;
      let r, l;
      if (a ? r = v.add(a.position, n.pointA) : r = n.pointA, n.render.type === "pin")
        s.beginPath(), s.arc(r.x, r.y, 3, 0, 2 * Math.PI), s.closePath();
      else {
        if (o ? l = v.add(o.position, n.pointB) : l = n.pointB, s.beginPath(), s.moveTo(r.x, r.y), n.render.type === "spring") {
          const c = v.sub(l, r), f = v.perp(v.normalise(c)), p = Math.ceil(m.clamp(n.length / 5, 12, 20));
          let u;
          for (let g = 1; g < p; g += 1)
            u = g % 2 === 0 ? 1 : -1, s.lineTo(r.x + c.x * (g / p) + f.x * u * 4, r.y + c.y * (g / p) + f.y * u * 4);
        }
        s.lineTo(l.x, l.y);
      }
      n.render.lineWidth && (s.lineWidth = n.render.lineWidth, s.strokeStyle = n.render.strokeStyle, s.stroke()), n.render.anchors && (s.fillStyle = n.render.strokeStyle, s.beginPath(), s.arc(r.x, r.y, 3, 0, 2 * Math.PI), s.arc(l.x, l.y, 3, 0, 2 * Math.PI), s.closePath(), s.fill());
    }
  }
  static bodies(t, e, s) {
    const i = s, n = t.options, a = n.showInternalEdges || !n.wireframes;
    let o, r, l, c;
    for (l = 0; l < e.length; l++)
      if (o = e[l], !!o.render.visible) {
        for (c = o.parts.length > 1 ? 1 : 0; c < o.parts.length; c++)
          if (r = o.parts[c], !!r.render.visible) {
            if (n.showSleeping && o.isSleeping ? i.globalAlpha = 0.5 * r.render.opacity : r.render.opacity !== 1 && (i.globalAlpha = r.render.opacity), r.render.sprite && r.render.sprite.texture && !n.wireframes) {
              const f = r.render.sprite, p = b._getTexture(t, f.texture);
              i.translate(r.position.x, r.position.y), i.rotate(r.angle), i.drawImage(p, p.width * -f.xOffset * f.xScale, p.height * -f.yOffset * f.yScale, p.width * f.xScale, p.height * f.yScale), i.rotate(-r.angle), i.translate(-r.position.x, -r.position.y);
            } else {
              if (r.circleRadius)
                i.beginPath(), i.arc(r.position.x, r.position.y, r.circleRadius, 0, 2 * Math.PI);
              else {
                i.beginPath(), i.moveTo(r.vertices[0].x, r.vertices[0].y);
                for (let f = 1; f < r.vertices.length; f++)
                  !r.vertices[f - 1].isInternal || a ? i.lineTo(r.vertices[f].x, r.vertices[f].y) : i.moveTo(r.vertices[f].x, r.vertices[f].y), r.vertices[f].isInternal && !a && i.moveTo(r.vertices[(f + 1) % r.vertices.length].x, r.vertices[(f + 1) % r.vertices.length].y);
                i.lineTo(r.vertices[0].x, r.vertices[0].y), i.closePath();
              }
              n.wireframes ? (i.lineWidth = 1, i.strokeStyle = t.options.wireframeStrokeStyle, i.stroke()) : (i.fillStyle = r.render.fillStyle, r.render.lineWidth && (i.lineWidth = r.render.lineWidth, i.strokeStyle = r.render.strokeStyle, i.stroke()), i.fill());
            }
            i.globalAlpha = 1;
          }
      }
  }
  static bodyWireframes(t, e, s) {
    const i = s, n = t.options.showInternalEdges;
    let a, o, r, l, c;
    for (i.beginPath(), r = 0; r < e.length; r++)
      if (a = e[r], !!a.render.visible)
        for (c = a.parts.length > 1 ? 1 : 0; c < a.parts.length; c++) {
          for (o = a.parts[c], i.moveTo(o.vertices[0].x, o.vertices[0].y), l = 1; l < o.vertices.length; l++)
            !o.vertices[l - 1].isInternal || n ? i.lineTo(o.vertices[l].x, o.vertices[l].y) : i.moveTo(o.vertices[l].x, o.vertices[l].y), o.vertices[l].isInternal && !n && i.moveTo(o.vertices[(l + 1) % o.vertices.length].x, o.vertices[(l + 1) % o.vertices.length].y);
          i.lineTo(o.vertices[0].x, o.vertices[0].y);
        }
    i.lineWidth = 1, i.strokeStyle = t.options.wireframeStrokeStyle, i.stroke();
  }
  static bodyConvexHulls(t, e, s) {
    const i = s;
    let n, a, o;
    for (i.beginPath(), a = 0; a < e.length; a++)
      if (n = e[a], !(!n.render.visible || n.parts.length === 1)) {
        for (i.moveTo(n.vertices[0].x, n.vertices[0].y), o = 1; o < n.vertices.length; o++) i.lineTo(n.vertices[o].x, n.vertices[o].y);
        i.lineTo(n.vertices[0].x, n.vertices[0].y);
      }
    i.lineWidth = 1, i.strokeStyle = "rgba(255,255,255,0.2)", i.stroke();
  }
  static vertexNumbers(t, e, s) {
    const i = s;
    let n, a, o;
    for (n = 0; n < e.length; n++) {
      const r = e[n].parts;
      for (o = r.length > 1 ? 1 : 0; o < r.length; o++) {
        const l = r[o];
        for (a = 0; a < l.vertices.length; a++)
          i.fillStyle = "rgba(255,255,255,0.2)", i.fillText(`${n}_${a}`, l.position.x + (l.vertices[a].x - l.position.x) * 0.8, l.position.y + (l.vertices[a].y - l.position.y) * 0.8);
      }
    }
  }
  static mousePosition(t, e, s) {
    const i = s;
    i.fillStyle = "rgba(255,255,255,0.8)", i.fillText(`${e.position.x}  ${e.position.y}`, e.position.x + 5, e.position.y - 5);
  }
  static bodyBounds(t, e, s) {
    const i = s, n = t.options;
    i.beginPath();
    for (let a = 0; a < e.length; a++) if (e[a].render.visible) {
      const o = e[a].parts;
      for (let r = o.length > 1 ? 1 : 0; r < o.length; r++) {
        const l = o[r];
        i.rect(l.bounds.min.x, l.bounds.min.y, l.bounds.max.x - l.bounds.min.x, l.bounds.max.y - l.bounds.min.y);
      }
    }
    n.wireframes ? i.strokeStyle = "rgba(255,255,255,0.08)" : i.strokeStyle = "rgba(0,0,0,0.1)", i.lineWidth = 1, i.stroke();
  }
  static bodyAxes(t, e, s) {
    const i = s, n = t.options;
    let a, o, r, l;
    for (i.beginPath(), o = 0; o < e.length; o++) {
      const c = e[o], f = c.parts;
      if (c.render.visible)
        if (n.showAxes) for (r = f.length > 1 ? 1 : 0; r < f.length; r++)
          for (a = f[r], l = 0; l < a.axes.length; l++) {
            const p = a.axes[l];
            i.moveTo(a.position.x, a.position.y), i.lineTo(a.position.x + p.x * 20, a.position.y + p.y * 20);
          }
        else for (r = f.length > 1 ? 1 : 0; r < f.length; r++)
          for (a = f[r], l = 0; l < a.axes.length; l++)
            i.moveTo(a.position.x, a.position.y), i.lineTo((a.vertices[0].x + a.vertices[a.vertices.length - 1].x) / 2, (a.vertices[0].y + a.vertices[a.vertices.length - 1].y) / 2);
    }
    n.wireframes ? (i.strokeStyle = "indianred", i.lineWidth = 1) : (i.strokeStyle = "rgba(255, 255, 255, 0.4)", i.globalCompositeOperation = "overlay", i.lineWidth = 2), i.stroke(), i.globalCompositeOperation = "source-over";
  }
  static bodyPositions(t, e, s) {
    const i = s, n = t.options;
    let a, o, r, l;
    for (i.beginPath(), r = 0; r < e.length; r++)
      if (a = e[r], !!a.render.visible)
        for (l = 0; l < a.parts.length; l++)
          o = a.parts[l], i.arc(o.position.x, o.position.y, 3, 0, 2 * Math.PI, !1), i.closePath();
    for (n.wireframes ? i.fillStyle = "indianred" : i.fillStyle = "rgba(0,0,0,0.5)", i.fill(), i.beginPath(), r = 0; r < e.length; r++)
      a = e[r], a.render.visible && (i.arc(a.positionPrev.x, a.positionPrev.y, 2, 0, 2 * Math.PI, !1), i.closePath());
    i.fillStyle = "rgba(255,165,0,0.8)", i.fill();
  }
  static bodyVelocity(t, e, s) {
    const i = s;
    i.beginPath();
    for (let n = 0; n < e.length; n++) {
      const a = e[n];
      if (!a.render.visible) continue;
      const o = q.getVelocity(a);
      i.moveTo(a.position.x, a.position.y), i.lineTo(a.position.x + o.x, a.position.y + o.y);
    }
    i.lineWidth = 3, i.strokeStyle = "cornflowerblue", i.stroke();
  }
  static bodyIds(t, e, s) {
    const i = s;
    let n, a;
    for (n = 0; n < e.length; n++) {
      if (!e[n].render.visible) continue;
      const o = e[n].parts;
      for (a = o.length > 1 ? 1 : 0; a < o.length; a++) {
        const r = o[a];
        i.font = "12px Arial", i.fillStyle = "rgba(255,255,255,0.5)", i.fillText(r.id, r.position.x + 10, r.position.y - 10);
      }
    }
  }
  static collisions(t, e, s) {
    const i = s, n = t.options;
    let a, o, r, l;
    for (i.beginPath(), r = 0; r < e.length; r++)
      if (a = e[r], !!a.isActive)
        for (o = a.collision, l = 0; l < a.contactCount; l++) {
          const c = a.contacts[l].vertex;
          i.rect(c.x - 1.5, c.y - 1.5, 3.5, 3.5);
        }
    for (n.wireframes ? i.fillStyle = "rgba(255,255,255,0.7)" : i.fillStyle = "orange", i.fill(), i.beginPath(), r = 0; r < e.length; r++)
      if (a = e[r], !!a.isActive && (o = a.collision, a.contactCount > 0)) {
        let c = a.contacts[0].vertex.x, f = a.contacts[0].vertex.y;
        a.contactCount === 2 && (c = (a.contacts[0].vertex.x + a.contacts[1].vertex.x) / 2, f = (a.contacts[0].vertex.y + a.contacts[1].vertex.y) / 2), o.bodyB === o.supports[0].body || o.bodyA.isStatic === !0 ? i.moveTo(c - o.normal.x * 8, f - o.normal.y * 8) : i.moveTo(c + o.normal.x * 8, f + o.normal.y * 8), i.lineTo(c, f);
      }
    n.wireframes ? i.strokeStyle = "rgba(255,165,0,0.7)" : i.strokeStyle = "orange", i.lineWidth = 1, i.stroke();
  }
  static separations(t, e, s) {
    const i = s, n = t.options;
    let a, o, r, l, c;
    for (i.beginPath(), c = 0; c < e.length; c++) {
      if (a = e[c], !a.isActive) continue;
      o = a.collision, r = o.bodyA, l = o.bodyB;
      let f = 1;
      !l.isStatic && !r.isStatic && (f = 0.5), l.isStatic && (f = 0), i.moveTo(l.position.x, l.position.y), i.lineTo(l.position.x - o.penetration.x * f, l.position.y - o.penetration.y * f), f = 1, !l.isStatic && !r.isStatic && (f = 0.5), r.isStatic && (f = 0), i.moveTo(r.position.x, r.position.y), i.lineTo(r.position.x + o.penetration.x * f, r.position.y + o.penetration.y * f);
    }
    n.wireframes ? i.strokeStyle = "rgba(255,165,0,0.5)" : i.strokeStyle = "orange", i.stroke();
  }
  static inspector(t, e) {
    const s = t.selected, i = t.render, n = i.options;
    let a;
    if (n.hasBounds) {
      const o = i.bounds.max.x - i.bounds.min.x, r = i.bounds.max.y - i.bounds.min.y, l = o / i.options.width, c = r / i.options.height;
      e.scale(1 / l, 1 / c), e.translate(-i.bounds.min.x, -i.bounds.min.y);
    }
    for (let o = 0; o < s.length; o++) {
      const r = s[o].data;
      switch (e.translate(0.5, 0.5), e.lineWidth = 1, e.strokeStyle = "rgba(255,165,0,0.9)", e.setLineDash([1, 2]), r.type) {
        case "body":
          a = r.bounds, e.beginPath(), e.rect(Math.floor(a.min.x - 3), Math.floor(a.min.y - 3), Math.floor(a.max.x - a.min.x + 6), Math.floor(a.max.y - a.min.y + 6)), e.closePath(), e.stroke();
          break;
        case "constraint": {
          let l = r.pointA;
          r.bodyA && (l = r.pointB), e.beginPath(), e.arc(l.x, l.y, 10, 0, 2 * Math.PI), e.closePath(), e.stroke();
          break;
        }
      }
      e.setLineDash([]), e.translate(-0.5, -0.5);
    }
    t.selectStart !== null && (e.translate(0.5, 0.5), e.lineWidth = 1, e.strokeStyle = "rgba(255,165,0,0.6)", e.fillStyle = "rgba(255,165,0,0.1)", a = t.selectBounds, e.beginPath(), e.rect(Math.floor(a.min.x), Math.floor(a.min.y), Math.floor(a.max.x - a.min.x), Math.floor(a.max.y - a.min.y)), e.closePath(), e.stroke(), e.fill(), e.translate(-0.5, -0.5)), n.hasBounds && e.setTransform(1, 0, 0, 1, 0, 0);
  }
  static _updateTiming(t, e) {
    const s = t.engine, i = t.timing, n = i.historySize, a = s.timing.timestamp;
    i.delta = e - i.lastTime || b._goodDelta, i.lastTime = e, i.timestampElapsed = a - i.lastTimestamp || 0, i.lastTimestamp = a, i.deltaHistory.unshift(i.delta), i.deltaHistory.length = Math.min(i.deltaHistory.length, n), i.engineDeltaHistory.unshift(s.timing.lastDelta), i.engineDeltaHistory.length = Math.min(i.engineDeltaHistory.length, n), i.timestampElapsedHistory.unshift(i.timestampElapsed), i.timestampElapsedHistory.length = Math.min(i.timestampElapsedHistory.length, n), i.engineUpdatesHistory.unshift(s.timing.lastUpdatesPerFrame), i.engineUpdatesHistory.length = Math.min(i.engineUpdatesHistory.length, n), i.engineElapsedHistory.unshift(s.timing.lastElapsed), i.engineElapsedHistory.length = Math.min(i.engineElapsedHistory.length, n), i.elapsedHistory.unshift(i.lastElapsed), i.elapsedHistory.length = Math.min(i.elapsedHistory.length, n);
  }
  static _mean(t) {
    let e = 0;
    for (let s = 0; s < t.length; s += 1) e += t[s];
    return e / t.length || 0;
  }
  static _createCanvas(t, e) {
    const s = document.createElement("canvas");
    return s.width = t, s.height = e, s.oncontextmenu = () => !1, s.onselectstart = () => !1, s;
  }
  static _getPixelRatio(t) {
    const e = t.getContext("2d");
    return (window.devicePixelRatio || 1) / (e.webkitBackingStorePixelRatio || e.mozBackingStorePixelRatio || e.msBackingStorePixelRatio || e.oBackingStorePixelRatio || e.backingStorePixelRatio || 1);
  }
  static _getTexture(t, e) {
    let s = t.textures[e];
    return s || (s = t.textures[e] = new Image(), s.src = e, s);
  }
  static _applyBackground(t, e) {
    let s = e;
    /(jpg|gif|png)$/.test(e) && (s = `url(${e})`), t.canvas.style.background = s, t.canvas.style.backgroundSize = "contain", t.currentBackground = e;
  }
}, ae = class $ {
  static _maxFrameDelta = 1e3 / 15;
  static _frameDeltaFallback = 1e3 / 60;
  static _timeBufferMargin = 1.5;
  static _elapsedNextEstimate = 1;
  static _smoothingLowerBound = 0.1;
  static _smoothingUpperBound = 0.9;
  constructor(t) {
    Object.assign(this, m.extend({
      delta: 1e3 / 60,
      frameDelta: null,
      frameDeltaSmoothing: !0,
      frameDeltaSnapping: !0,
      frameDeltaHistory: [],
      frameDeltaHistorySize: 100,
      frameRequestId: null,
      timeBuffer: 0,
      timeLastTick: null,
      maxUpdates: null,
      maxFrameTime: 1e3 / 30,
      lastUpdatesDeferred: 0,
      enabled: !0
    }, t)), this.fps = 0;
  }
  static create(t) {
    return new $(t);
  }
  static run(t, e) {
    return t.timeBuffer = $._frameDeltaFallback, (function s(i) {
      t.frameRequestId = $._onNextFrame(t, s), i && t.enabled && $.tick(t, e, i);
    })(), t;
  }
  static tick(t, e, s) {
    const i = m.now(), n = t.delta;
    let a = 0, o = s - t.timeLastTick;
    if ((!o || !t.timeLastTick || o > Math.max($._maxFrameDelta, t.maxFrameTime)) && (o = t.frameDelta || $._frameDeltaFallback), t.frameDeltaSmoothing) {
      t.frameDeltaHistory.push(o), t.frameDeltaHistory = t.frameDeltaHistory.slice(-t.frameDeltaHistorySize);
      const f = t.frameDeltaHistory.slice(0).sort(), p = t.frameDeltaHistory.slice(f.length * $._smoothingLowerBound, f.length * $._smoothingUpperBound);
      o = $._mean(p) || o;
    }
    t.frameDeltaSnapping && (o = 1e3 / Math.round(1e3 / o)), t.frameDelta = o, t.timeLastTick = s, t.timeBuffer += t.frameDelta, t.timeBuffer = m.clamp(t.timeBuffer, 0, t.frameDelta + n * $._timeBufferMargin), t.lastUpdatesDeferred = 0;
    const r = t.maxUpdates || Math.ceil(t.maxFrameTime / n), l = { timestamp: e.timing.timestamp };
    E.trigger(t, "beforeTick", l), E.trigger(t, "tick", l);
    const c = m.now();
    for (; n > 0 && t.timeBuffer >= n * $._timeBufferMargin; ) {
      E.trigger(t, "beforeUpdate", l), Xt.update(e, n), E.trigger(t, "afterUpdate", l), t.timeBuffer -= n, a += 1;
      const f = m.now() - i, p = m.now() - c, u = f + $._elapsedNextEstimate * p / a;
      if (a >= r || u > t.maxFrameTime) {
        t.lastUpdatesDeferred = Math.round(Math.max(0, t.timeBuffer / n - $._timeBufferMargin));
        break;
      }
    }
    e.timing.lastUpdatesPerFrame = a, E.trigger(t, "afterTick", l), t.frameDeltaHistory.length >= 100 && (t.lastUpdatesDeferred && Math.round(t.frameDelta / n) > r ? m.warnOnce("Matter.Runner: runner reached runner.maxUpdates, see docs.") : t.lastUpdatesDeferred && m.warnOnce("Matter.Runner: runner reached runner.maxFrameTime, see docs."), typeof t.isFixed < "u" && m.warnOnce("Matter.Runner: runner.isFixed is now redundant, see docs."), (t.deltaMin || t.deltaMax) && m.warnOnce("Matter.Runner: runner.deltaMin and runner.deltaMax were removed, see docs."), t.fps !== 0 && m.warnOnce("Matter.Runner: runner.fps was replaced by runner.delta, see docs."));
  }
  static stop(t) {
    $._cancelNextFrame(t);
  }
  static _onNextFrame(t, e) {
    if (typeof window < "u" && window.requestAnimationFrame) t.frameRequestId = window.requestAnimationFrame(e);
    else throw new Error("Matter.Runner: missing required global window.requestAnimationFrame.");
    return t.frameRequestId;
  }
  static _cancelNextFrame(t) {
    if (typeof window < "u" && window.cancelAnimationFrame) window.cancelAnimationFrame(t.frameRequestId);
    else throw new Error("Matter.Runner: missing required global window.cancelAnimationFrame.");
  }
  static _mean(t) {
    let e = 0;
    const s = t.length;
    for (let i = 0; i < s; i += 1) e += t[i];
    return e / s || 0;
  }
}, oe = m.deprecated, zt = class {
  static collides(D, t) {
    return vt.collides(D, t);
  }
};
oe(zt, "collides", "SAT.collides ➤ replaced by Collision.collides");
var re = class Gt {
  static pathToVertices(t, e = 15) {
    typeof window < "u" && !("SVGPathSeg" in window) && m.warn("Svg.pathToVertices: SVGPathSeg not defined, a polyfill is required.");
    let s, i, n, a, o, r, l, c, f, p, u, g, y = 0, h = 0, d = 0;
    const x = [], S = (A, k, I) => {
      const F = I % 2 === 1 && I > 1;
      if (!f || A != f.x || k != f.y) {
        f && F ? (u = f.x, g = f.y) : (u = 0, g = 0);
        const L = {
          x: u + A,
          y: g + k
        };
        (F || !f) && (f = L), x.push(L), h = u + A, d = g + k;
      }
    }, P = (A) => {
      const k = A.pathSegTypeAsLetter.toUpperCase();
      if (k !== "Z") {
        switch (k) {
          case "M":
          case "L":
          case "T":
          case "C":
          case "S":
          case "Q":
            h = A.x, d = A.y;
            break;
          case "H":
            h = A.x;
            break;
          case "V":
            d = A.y;
            break;
        }
        S(h, d, A.pathSegType);
      }
    };
    for (Gt._svgPathToAbsolute(t), n = t.getTotalLength(), r = [], s = 0; s < t.pathSegList.numberOfItems; s += 1) r.push(t.pathSegList.getItem(s));
    for (l = r.concat(); y < n; ) {
      if (p = t.getPathSegAtLength(y), o = r[p], o != c) {
        for (; l.length && l[0] != o; ) P(l.shift());
        c = o;
      }
      switch (o.pathSegTypeAsLetter.toUpperCase()) {
        case "C":
        case "T":
        case "S":
        case "Q":
        case "A":
          a = t.getPointAtLength(y), S(a.x, a.y, 0);
          break;
      }
      y += e;
    }
    for (s = 0, i = l.length; s < i; ++s) P(l[s]);
    return x;
  }
  static _svgPathToAbsolute(t) {
    let e, s, i, n, a, o, r = 0, l = 0;
    const c = t.pathSegList, f = c.numberOfItems;
    for (let p = 0; p < f; ++p) {
      const u = c.getItem(p), g = u.pathSegTypeAsLetter;
      if (/[MLHVCSQTA]/.test(g))
        "x" in u && (r = u.x), "y" in u && (l = u.y);
      else
        switch ("x1" in u && (i = r + u.x1), "x2" in u && (a = r + u.x2), "y1" in u && (n = l + u.y1), "y2" in u && (o = l + u.y2), "x" in u && (r += u.x), "y" in u && (l += u.y), g) {
          case "m":
            c.replaceItem(t.createSVGPathSegMovetoAbs(r, l), p);
            break;
          case "l":
            c.replaceItem(t.createSVGPathSegLinetoAbs(r, l), p);
            break;
          case "h":
            c.replaceItem(t.createSVGPathSegLinetoHorizontalAbs(r), p);
            break;
          case "v":
            c.replaceItem(t.createSVGPathSegLinetoVerticalAbs(l), p);
            break;
          case "c":
            c.replaceItem(t.createSVGPathSegCurvetoCubicAbs(r, l, i, n, a, o), p);
            break;
          case "s":
            c.replaceItem(t.createSVGPathSegCurvetoCubicSmoothAbs(r, l, a, o), p);
            break;
          case "q":
            c.replaceItem(t.createSVGPathSegCurvetoQuadraticAbs(r, l, i, n), p);
            break;
          case "t":
            c.replaceItem(t.createSVGPathSegCurvetoQuadraticSmoothAbs(r, l), p);
            break;
          case "a":
            c.replaceItem(t.createSVGPathSegArcAbs(r, l, u.r1, u.r2, u.angle, u.largeArcFlag, u.sweepFlag), p);
            break;
          case "z":
          case "Z":
            r = e, l = s;
            break;
        }
      (g == "M" || g == "m") && (e = r, s = l);
    }
  }
}, le = class {
  static create = R.create;
  static add = R.add;
  static remove = R.remove;
  static clear = R.clear;
  static addComposite = R.addComposite;
  static addBody = R.addBody;
  static addConstraint = R.addConstraint;
};
V.Axes = _;
V.Bodies = qt;
V.Body = q;
V.Bounds = H;
V.Collision = vt;
V.Common = m;
V.Composite = R;
V.Composites = ee;
V.Constraint = X;
V.Contact = It;
V.Detector = nt;
V.Engine = Xt;
V.Events = E;
V.Mouse = z;
V.MouseConstraint = ie;
V.Pair = rt;
V.Pairs = xt;
V.Plugin = Ht;
V.Query = se;
V.Render = ne;
V.Resolver = it;
V.Runner = ae;
V.SAT = zt;
V.Sleeping = Z;
V.Svg = re;
V.Vector = v;
V.Vertices = C;
V.World = le;
var ce = V;
export {
  _ as Axes,
  qt as Bodies,
  q as Body,
  H as Bounds,
  vt as Collision,
  m as Common,
  R as Composite,
  ee as Composites,
  X as Constraint,
  It as Contact,
  nt as Detector,
  Xt as Engine,
  E as Events,
  z as Mouse,
  ie as MouseConstraint,
  rt as Pair,
  xt as Pairs,
  Ht as Plugin,
  se as Query,
  ne as Render,
  it as Resolver,
  ae as Runner,
  zt as SAT,
  Z as Sleeping,
  re as Svg,
  v as Vector,
  C as Vertices,
  le as World,
  ce as default
};

//# sourceMappingURL=matter.min.js.map